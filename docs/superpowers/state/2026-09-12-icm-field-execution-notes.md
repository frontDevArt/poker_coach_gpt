# Журнал исполнения плана 3 — модель поля, лесенка, PKO, бюджет 2 секунды

План: `docs/superpowers/plans/2026-09-12-icm-field-model.md`.
Спека: `docs/superpowers/specs/2026-09-12-icm-field-model-design.md`.
Фазы, правила сессий и реестр долгов: `docs/superpowers/state/2026-09-24-plan3-phases.md`.

Каждая фаза дописывает сюда раздел `## Фаза N` по §1.7 документа фаз. Итоговый раздел пишет
Фаза 10.

## До фаз: Задача 1 (2026-09-12 — 2026-09-13)

Задача 1 исполнена по старому циклу, где каждый прогон шёл отдельной сессией, прямо в `main`.
Решения каждого прогона записаны в тексте Задачи 1 плана.

| Коммит | Что |
|---|---|
| `472021d` | реализация: `ladder.py`, `tests/test_ladder.py` |
| `85e81aa` | прогон A: тексты из `handstate`, гарды, без материализации |
| `42d9e99` | прогон B: `bool`, мёртвый гард, порядок отказов |
| `ddf7478` | прогон C: NaN и `inf` в общих гардах, границы лесенки |
| `d686089` | прогон D: NaN своим текстом, общие гарды в `bounty.py`, ключ сортировки |

Тестов после прогона D, по записи плана, — 357. Ответа ревью `НАХОДОК НЕТ` после прогона D нет:
ревью прогона E делает Фаза 1.

## Фаза 0 — нарезка на фазы (2026-09-24)

Сессия кода не трогала.

- Восстановлено состояние: план 3 начат, Задача 1 прошла прогоны A–D, Задачи 2–8 не начаты.
- `CLAUDE.md` приведён к текущему состоянию (`a68f594`, `5e97cef`) и влит в `main`.
- Ветки `feat/poker-engine-core` и `feat/screenshot-coach-engine` проверены: обе целиком в
  `main`. Удалить их из облачной сессии не удалось: `git push --delete` обрывается сервером.
  Это долг D15, за пользователем.
- По решению пользователя остаток плана нарезан на десять фаз, одна фаза — одна сессия до
  ~150k. Документ фаз заменяет раздел «Цикл исполнения» плана.
- Аудит долгов: журнал плана 2, `HANDOFF.md`, спека §11 и перекрёстные ссылки задач. Итог —
  реестр D1–D16 с хозяевами в документе фаз, §5.
- Тесты в этой сессии не гонялись: в облачном контейнере нет `.venv` и `pokerkit`. Базовое
  число снимает Фаза 1.

Следующая — Фаза 1, промпт в `docs/superpowers/state/plan3-next-prompt.md`.

## Фаза 1 — ревью Задачи 1 (прогон E) и Задача 2 (2026-09-24)

Ветка: облачная сессия навязала `claude/serene-bell-yni6li` (§3 документа фаз), работа идёт в
ней; `feat/icm-field-model` не создавалась. Linux, команды через `.venv/bin/python` (D16).

### База

`.venv` создан с нуля по `requirements-dev.lock` на Python 3.11.15. Собрано 357 тестов
(совпадает с записью плана), **356 passed, 1 failed**:
`test_cli.py::test_error_json_is_valid_utf8_on_subprocess_console` зовёт
`Path(sys.executable).with_name("poker-engine.exe")` — на Linux консольный скрипт называется
`poker-engine`, без `.exe`. Это не регрессия кода, а непереносимый тест; см. «Среда» ниже.

### Ревью Задачи 1, прогон E

Объём: `472021d`, `85e81aa`, `42d9e99`, `ddf7478`, `d686089`; текущие `ladder.py`,
`_checks.py`, гард-обёртки `bounty.py`, `tests/test_ladder.py`, `tests/test_checks.py`,
гард-тесты `tests/test_bounty.py`. Сверка с Задачей 1 (решения A–D), спекой §4 и §9.

- **Спека.** §4.4: интервалы без материализации, `is_complete` даёт полноту под пометку
  `ladder_incomplete`. Расхождений нет.
- **Global Constraints.** Имена гардов мужского рода (`номер места в выплатах`,
  `размер призовой зоны`, `приз за место N`, `номер места`); копий гардов нет; тексты
  `handstate` дословно (сверено с `handstate.py:383–425`).
- **Приёмка.** п.1 — `test_places_past_the_ladder_pay_nothing`; п.2 — `_total` считан в
  конструкторе, `test_total_on_an_incomplete_ladder_counts_only_covered_places`; п.3 — тексты
  сверены построчно; п.5 — `test_intervals_touching_on_a_shared_bound_are_rejected`; п.6 —
  `test_memory_does_not_scale_with_places_paid`; п.8 — см. базу. п.4 и п.3 (docstring) — находки.
- **Мутационная приёмка (п.7).** 39 мутантов, по одному, откат `git checkout --`; прогон
  `test_ladder`, `test_checks`, `test_bounty`, `test_handstate`, `test_profiles`, `test_cli`.
  **Все 39 убиты.** `ladder.py` (26): снос каждого из 9 гардов (`if not intervals`, два
  `check_integer` границ, `raise` интервала, `check_positive` приза, `raise` пересечения,
  `check_integer`/`check_positive` `places_paid`, `raise` глубины), два `check_*` в `prize`;
  `last < first - 1`, `first < 0`, `max → min`, `> → >=` глубины, `<= → <` пересечения и в
  `prize`, `bisect_left`, снос `at >= 0`, снос `self._ends and`, `== → !=` в `is_complete`,
  ключ `i[1]`, снос сортировки, снос `float(amount)`, `covered` без `+1`, `total` без
  множителя. `_checks.py` (10): снос `_check_number` и `_check_finite` в обоих гардах,
  `<= ↔ <`, снос проверки NaN и `inf`, снос каждого подусловия `check_integer`. `bounty.py`
  (3): снос каждой из трёх обёрток. Эквивалентный `== → >=` в `is_complete` записан в коде.

**Находки:**

1. **E1** — `ladder.py`, docstring модуля (абзац «Порядок гардов тоже повторяет…») и решение
   B.3 в плане. Обещание «приоритет сообщений на данных, нарушающих сразу два правила,
   обязан остаться прежним», когда Задача 4 переведёт ладдер-блок `_validate_context` на
   класс, построением класса не выполнимо. В `_validate_context` (`handstate.py:386–425`)
   проверка монотонности призов стоит **внутри** цикла по интервалам, а проверка
   `entrants < players_left` — **между** циклом и `places_paid`. Класс не делает ни той, ни
   другой, поэтому на данных «монотонность нарушена на интервале i, интервал i+1 кривой»
   сегодня выходит текст монотонности, а после Задачи 4 выйдет текст интервала; так же для
   пары «входов меньше, чем осталось» + «`places_paid` ≤ 0». Ни один тест `handstate` такие
   пары не пинит. Как чинить: сузить docstring до того, что правда — порядок **собственных**
   гардов класса совпадает с `_validate_context`; назвать два места, где приоритет не
   сохраняется; выбор для Задачи 4 записать долгом с хозяином Фаза 3.
2. **E2** — `tests/test_ladder.py::test_fractional_interval_bound_is_rejected`. Приёмка п.4:
   «ни один текст не бросается двумя разными гардами модуля». Текст
   `номер места в выплатах должен быть целым` бросают два гарда — на `first` и на `last`, — а
   тест пинит его без значения, так что по тесту гарды неразличимы. Мутанты сноса каждого
   убиты, но не по тексту. Как чинить: текст у двух границ различается значением
   (`получено 1.5` / `получено 2.5`) — пинить его вместе со значением и якорем `$`.

Рассмотрено и не находка: `total()` переполняется до `inf` при призах порядка `1e308` —
такой приз не приходит ни со скриншота, ни из CLI, а каждое слагаемое конечно и проходит гард;
`check_probability` на NaN отвечает своим текстом диапазона — модуль Задачей 1 не менялся.

**Что сделано с находками** (коммит `2c6dbb8`, одна починка):

- E1 — починено. Docstring `ladder.py` сужен до правды: порядок собственных гардов класса;
  два места, где приоритет валидатора не сохранится, названы. Решение B.3 сужено блоком
  «прогон E» в тексте Задачи 1. Выбор порядка для Задачи 4 — новый долг **D17**, хозяин Фаза 3.
- E2 — починено. Оба случая пинятся со значением и `$`. Мутант «`check_integer` на `last`
  заменён вторым `check_integer` на `first`» краснит тест по тексту (проверено).

Чекпоинт §1.3: две находки, один коммит починки — реализация Задачи 2 начата.

### Среда (не находка ревью)

`dac3376` — `test(engine)`: `test_cli.py` ищет `poker-engine.exe` только на Windows, иначе
`poker-engine`. Долг **D18**, заведён и закрыт в этой фазе. Отдельный коммит, а не часть
починки: к ревью Задачи 1 он не относится, и чекпоинт на него не распространяется.

### Задача 2

Коммит `9ea17e0` — `feat(engine): ICM по столу поимённо и полю счётчиком`,
`icm_field.py` и `tests/test_icm_field.py`, 19 тестов. Решения против наброска плана записаны
в тексте Задачи 2 блоком «Решения при реализации (Фаза 1)». Кратко:

1. `check_integer` на `field_count` (обещание B.1).
2. Стек поля при пустом поле не читается: в наброске `0 · nan` отравлял все эквити.
3. Фишки живых — сумма оставшихся стеков `chips[full ^ mask] + (F − k) · a`, без вычитания;
   мёртвый гард `remaining <= 0` наброска убран.
4. Ряд «стол выбыл целиком» отдаёт полю хвост лесенки, без деления на нулевые фишки.
5. Призы — один запрос `ladder.prize` на место.
6. Инвариант 1 уточнён: сумма равна лесенке по местам `1..t + F`; тест на места, уже
   выплаченные выбывшим.

Приёмка Задачи 2, по пунктам:

1. `test_an_empty_field_reproduces_the_exact_enumeration` — `abs=1e-12`, не ослаблен.
2. `test_money_is_never_lost` — сумма равна `ladder.total()`.
3. `grep MAX_TABLE_SEATS src/` — только `icm_field.py`.
4. `field_count` входит лишь в длину рядов `reach` (`F + 1`), экспонента — `1 << seats`.
5. Отказов пять (четыре плановых плюс дробный размер поля), все с `match=`; имена `стек на
   месте N`, `размер поля`, `стек игрока поля` — мужского рода.
6. Сьют зелёный, см. ниже.

Самопроверка мутантами (не замена ревью Фазы 2): 26 мутантов на `test_icm_field.py`, 24 убиты,
два выживших эквивалентны — снос `probability == 0.0` (ускорение, записано комментарием) и
лишний элемент в списке призов, который никто не читает.

Замеры (контейнер, Python 3.11, лесенка 16 строк, поле 490): 6 мест — 0.04 с, 8 — 0.14 с,
9 — 0.29 с, 10 — 0.61 с; 6 мест и поле 900 — 0.08 с.

### Коммиты фазы

| Коммит | Что |
|---|---|
| `2c6dbb8` | `fix(engine)`: находки E1, E2 прогона E Задачи 1 |
| `dac3376` | `test(engine)`: тест CLI без `.exe` вне Windows (D18) |
| `9ea17e0` | `feat(engine)`: Задача 2, `icm_field.py` |
| этот | `docs`: отчёт, реестр, промпт Фазы 2 |

### Тесты (D1)

До: 357 собрано, 356 passed, 1 failed (`.exe`, среда). После: **376 passed** (357 + 19
`test_icm_field.py`), 3 мин 54 с. Полных прогонов за фазу — два.

### Долги

- Закрыты: D18 (`dac3376`). D1 и D16 — выполнены в этой фазе, остаются правилами для следующих.
- Новые: **D17** — приоритет отказов `_validate_context` после перевода на `PayoutLadder`,
  хозяин Фаза 3 (тем же коммитом Задачи 4).
- Дополнен **D3**: `table_equities` требует стек стола `> 0`, а `handstate` пропускает
  `0` (`check_non_negative`), так что олл-ин 0 BB упрётся в гард `стек на месте N` Задачи 2.
  Номер в этом тексте — позиция в списке `table` (с нуля), а у `handstate` тот же шаблон
  несёт `seat_index`. Задача 4 обязана либо подавать стеки так, чтобы номера совпали, либо
  не пускать ноль до расчёта.

### Где остановились

Фаза доделана целиком. Следующая — Фаза 2: ревью Задачи 2 и починок прогона E (`2c6dbb8`,
`dac3376`), затем Задача 3.

## Фаза 2 — ревью Задачи 2 и Задача 3 (2026-09-24)

Ветка `claude/serene-bell-yni6li` (облачная сессия, §3), Linux, `.venv/bin/python` (D16).
`.venv` уже был в контейнере.

### Ревью Задачи 2 (`9ea17e0`), починок прогона E (`2c6dbb8`) и `dac3376`

Объём: `icm_field.py`, `tests/test_icm_field.py`, блок «Решения при реализации (Фаза 1)» в
тексте Задачи 2; диффы `2c6dbb8` (`ladder.py`, `test_ladder.py`) и `dac3376` (`test_cli.py`).

- **Спека §3.1.** Переходы и разыгрываемое место `|S| + k + 1` совпадают с кодом
  (`prizes[seats_gone + k]`, индекс с нуля). Решение 3 (фишки живых суммой, а не вычитанием)
  переформулирует `remaining` спеки без смены смысла. Независимая сверка: 200 случайных спотов
  (`t` 1–4, `F` 0–4, случайные невозрастающие лесенки, в том числе короче и длиннее `t + F`),
  поле развёрнуто в именованных игроков и посчитано `icm.icm_equities` — наибольшее
  расхождение `3.3e-12` на эквити порядка сотен. Модель верна и при `F ≥ 2`, где тесты
  сверяют её с перебором только косвенно.
- **Спека §9.** Инварианты 1, 2, 3, 4, 5, 6 для `table_equities` запинены
  (`test_money_is_never_lost`, `…_scales_every_equity`, `…_changes_nothing`,
  `…_reproduces_the_exact_enumeration`, `…_worth_more_but_less_than_proportionally`,
  `…_split_the_pool_evenly`).
- **Global Constraints.** Один коммит задачи; гарды только из `_checks.py`; имена
  `стек на месте N`, `размер поля`, `стек игрока поля` — мужского рода; все отказы с `match=`.
- **Приёмка.** п.1 — `abs=1e-12`; п.2 — `sum(seats) + field == ladder.total()`; п.3 —
  `grep -rn MAX_TABLE_SEATS src/` даёт только `icm_field.py`; п.4 — `1 << seats`, `field_count`
  только в длине рядов; п.5 — пять отказов, каждый с `match=`; п.6 — база ниже.
- **Мутационная приёмка.** 25 мутантов по одному, откат `git checkout --`, прогон
  `test_icm_field.py`. Снос каждого из семи гардов (пустой стол, предел, стек места,
  `check_integer` и `check_non_negative` поля, стек поля, условие `field_count > 0` → `>= 0`),
  `> → >=` предела, чтение стека пустого поля, `taken` без `+1`, сдвиг и потеря `k` в индексе
  приза, обход ряда `full` в главном цикле, знаменатель без поля, шаг поля за одного игрока,
  снос вклада поля, `table_left` по полному столу, хвост без `k = 0`, сдвиг хвоста, хвост без
  вероятности, снос хвоста, приз мест без приза, перенос поля в тот же `k` — **23 убиты**.
  Выжили два эквивалентных: лишний элемент списка призов (его никто не читает, отмечено
  Фазой 1) и `if left:` → `if left >= 0 and k < field_count:` (то же условие).
- **`2c6dbb8`.** Docstring `ladder.py` сверен с `handstate._validate_context`
  (`handstate.py:383–425`): монотонность — внутри цикла, `entrants < players_left` — между
  циклом и `places_paid`, у класса нет ни того, ни другого; утверждение верно. E2: оба текста
  границ пинятся значением и `$`. Замечено и не находка: `.` в `1.5$` — метасимвол регэкспа,
  но другого числа, совпадающего с шаблоном, гарды не выдают.
- **`dac3376`.** Имя консольного скрипта по `sys.platform`; на Linux тест зелёный в базе.

**Находки:**

1. **F2.1** — `icm_field.py:17–19`, docstring модуля: «поле в тысячу человек считается так же
   дёшево, как в сто». Неправда: стоимость `2^t × (F+1)` линейна по `F`. Замер: 6 мест,
   поле 100 — 0.006 с, 1000 — 0.061 с, 5000 — 0.27 с. Для Задачи 8 это не мелочь: ранняя
   стадия MTT даёт `players_left` в тысячи, и ложное обещание прячет статью бюджета. Как
   чинить: сказать правду — экспонента только от стола, поле входит линейно, множителем
   `F + 1`; поле в тысячу стоит вдесятеро дороже, чем в сто, а не в `2^900` раз.

**Что сделано с находками** (коммит `0f5336e`, одна починка):

- F2.1 — починено. Docstring `icm_field.py`: экспонента только от стола, поле входит
  линейно, множителем `F + 1`; поле в тысячу вдесятеро дороже, чем в сто. Решение дописано в
  текст Задачи 2 блоком «Решения по находкам ревью (Фаза 2)».

Чекпоинт §1.3: одна находка, один коммит — реализация Задачи 3 начата.

### Задача 3

Коммит `c23a83c` — `feat(engine): риск-премия и bubble factor на модели поля`,
`icm_field.py` (`hero_equity`, `risk_premium`, `bubble_factor`, `_branches`, `_equity_after`,
`_check_seat`, `_CHIP_THRESHOLD`) и `tests/test_icm_field_pressure.py`, 18 тестов. Решения
против наброска — блок «Решения при реализации (Фаза 2)» в тексте Задачи 3. Кратко:

1. Параметра `at_risk` нет: его никто не передаёт, а значение больше эффективного стека
   создавало бы фишки. Набросок Задачи 8 (`pressure`) поправлен под это.
2. «Эффективный стек равен нулю» — на нулевом стеке соперника; порядок гардов как в
   `icm._icm_branches`.
3. Тест наброска на нулевую лесенку невозможен (`PayoutLadder` отвергает приз 0) — заменён
   лесенкой, платящей только за недостижимое место.
4. `_equity_after` без короткого пути «никто не вылетел» (эквивалентный мутант).
5. `_CHIP_THRESHOLD = 0.5` — константа с выводом.
6. 18 тестов вместо 10: бабл на бумаге (1/6 и 2) со столом и с полем, вылет героя при поле,
   вылет соперника раньше героя, лесенка, награждающая вылет, `villain` и `hero = -1`.

Приёмка Задачи 3, по пунктам:

1. Тексты `исход олл-ина не меняет ICM-эквити героя, risk premium не определён`,
   `выигрыш не увеличивает ICM-эквити, bubble factor не определён`, `hero и villain должны
   различаться`, `эффективный стек равен нулю`, `{name}={seat} вне диапазона игроков [0, n]`
   совпадают с `icm.py:85,115,132–139` дословно и запинены `^…$`.
2. `_equity_after` не сдвигает лесенку: вылетевший герой — `ladder.prize(живые + поле + 1)`
   (`test_a_busted_hero_takes_the_last_place_among_everyone_left`, мутанты `+0`, `+2`, «без
   поля» убиты); вылет соперника — лесенка целиком (`test_a_busted_villain_…`).
3. `_CHIP_THRESHOLD = 0.5` с выводом `e / 2e` в комментарии; мутант «без порога» убит.
4. `test_pressure_is_higher_on_the_bubble_than_deep_in_the_money` — две лесенки при одной
   раздаче.
5. Сьют зелёный, см. ниже.

Самопроверка мутантами (не замена ревью Фазы 3): 31 мутант, 30 убиты — снос каждого из
пяти гардов (`hero`, `villain`, `hero_equity`, различие, ставка), обе границы `_check_seat`,
`min → max`, ветви плоской лесенки обеих функций, `isclose → ==`, порог, отношение в
`bubble_factor`, место вылетевшего (`+0`, `+2`, без поля), снос ветви вылета, `>= 0` в
подсчёте живых, стол без сжатия, ставка не прибавлена/не вычтена, ветви перепутаны, индексы
`now`/`hero_equity`/после вылета, поле потеряно после вылета. Выжил снос
`isclose(money_up, 0)` в гарде `bubble_factor` — различим только на лесенке с призами
`< 1e-9`; строка из `icm.py` дословно; долг **D19**.

Инцидент: первый прогон мутантов откатывал файл `git checkout --`, а реализация ещё не была
закоммичена — рабочая копия откатилась к HEAD. Результаты мутантов при этом валидны (каждый
накладывался на копию в памяти), реализация восстановлена из того же текста. Дальше
харнесс откатывал к сохранённой копии. Урок для следующих фаз: мутанты — только на
закоммиченном файле или с откатом к копии.

### Коммиты фазы

| Коммит | Что |
|---|---|
| `0f5336e` | `fix(engine)`: находка F2.1 ревью Задачи 2 |
| `c23a83c` | `feat(engine)`: Задача 3, давление лесенки на модели поля |
| этот | `docs`: отчёт, реестр, промпт Фазы 3 |

### Тесты (D1)

До: 376 passed. После: **394 passed** (376 + 18 `test_icm_field_pressure.py`), ~4 мин.
Полных прогонов за фазу — два.

### Долги

- Закрытых нет. D1 и D16 выполнены в этой фазе, остаются правилами.
- Новый: **D19** — абсолютный допуск `1e-9` в `risk_premium`/`bubble_factor` ломает
  инвариант масштаба на лесенке с призами < 1e-9; хозяин — финал, в ledger.
- Дополнен **D3**: стек 0 у героя или соперника в `icm_field.risk_premium`/`bubble_factor`
  даёт `эффективный стек равен нулю`, у третьего места — `стек на месте N`.
- Карточка Фазы 3 дополнена ревью починки `0f5336e`.

### Где остановились

Фаза доделана целиком. Следующая — Фаза 3: ревью Задачи 3 (`c23a83c`) и починки F2.1
(`0f5336e`), затем Задача 4.

## Фаза 3 — ревью Задачи 3 и Задача 4 (2026-09-24)

Ветка `claude/serene-bell-yni6li` (облачная сессия, §3), Linux, `.venv/bin/python` (D16),
`.venv` уже был в контейнере.

### Ревью Задачи 3 (`c23a83c`) и починки F2.1 (`0f5336e`)

Объём: `icm_field.py` (`hero_equity`, `risk_premium`, `bubble_factor`, `_branches`,
`_equity_after`, `_check_seat`, `_CHIP_THRESHOLD`), `tests/test_icm_field_pressure.py`, блок
«Решения при реализации (Фаза 2)» в тексте Задачи 3; сверка с `icm.py:65–175`.

- **Спека §9.** Инв. 2 (масштаб лесенки не меняет давление) —
  `test_scaling_the_ladder_leaves_pressure_untouched`; инв. 7 (плоская лесенка не давит) —
  `test_a_flat_ladder_paying_everyone_does_not_press`, с полем; инв. 8 (бабл) —
  `test_pressure_is_higher_on_the_bubble_than_deep_in_the_money`. Инв. 3 для давления
  следует из инв. 3 `table_equities` (ставка — `min` двух стеков, масштабируется с ними).
- **Бумажные тесты пересчитаны вручную.** Трое равных, платят двоим: сейчас `2/3 × 50`,
  выигрыш 50, проигрыш 0, риск-премия `2/3 − 1/2 = 1/6`, bubble factor
  `(100/3)/(50/3) = 2` — сходится, и для стола, и для третьего в поле. «Лесенка награждает
  вылет» (платят только за 3-е): сейчас 30, выигрыш 0, проигрыш 90, риск-премия
  `(−60)/(−90) − 1/2 = 1/6` — сходится. Вылет соперника, сидящего раньше героя: проигрыш
  героя никого не выбивает (`40 − 20 = 20 > 0`) — сходится.
- **Сверка с `icm.py`.** Порядок гардов `_branches` = `icm._icm_branches` (места, различие,
  эффективный стек, затем расчёт); тексты пяти отказов дословны; ветви плоской лесенки обеих
  функций дословны. Решение 1 (нет `at_risk`) верно: значение больше `min` создавало бы фишки.
- **Global Constraints.** Коммит задачи один (`c23a83c` включает и блок решений в плане —
  так же делали Задачи 1–2); гарды — `_check_seat` локален (не общий: текст с именем параметра
  и диапазоном, как в `icm.py`); все отказы с `^…$`.
- **Приёмка.** п.1 — тексты дословны, пины `^…$`; п.2 — `ladder.prize(alive + field_count + 1)`,
  лесенка не сдвигается; п.3 — `_CHIP_THRESHOLD` с выводом `e / 2e`; п.4 — две лесенки при одной
  раздаче; п.5 — база 394.
- **Мутационная приёмка.** 32 мутанта по одному на закоммиченном файле, откат к копии в
  памяти, прогон `test_icm_field_pressure.py` + `test_icm_field.py`. Класс «снести гард
  целиком»: `_check_seat` в `hero_equity`, гарды `hero`, `villain`, различия, ставки,
  `bubble_factor` целиком — все убиты. Прочие: `<=`→`<` ставки, обе границы `_check_seat`,
  `min→max`, ветвь плоской лесенки RP без проверки `now`, `isclose→==`, порог `0.5→0`,
  переставленный числитель, ветвь `1.0` BF снесена и без проверки `now`, снос `money_up<=0`,
  отношение BF перевёрнуто, место вылетевшего `+0`/`+2`/без поля, снос ветви вылета,
  `>= 0` в подсчёте живых, стол без сжатия, ставка не прибавлена/не отнята, ветви
  перепутаны, `now` по сопернику, поле потеряно после вылета (в `_equity_after` и в ветви
  выигрыша), индекс героя без пересчёта — **31 убит**. Выжил `isclose(now, 0) → now != 0`
  в ветви плоской лесенки RP: различим только при `0 < now < 1e-9`, тот же класс, что D19.
- **F2.1 (`0f5336e`).** Docstring: `2^t × (F+1)` — линейно по `F`, `1001/101 ≈ 9.9`
  («вдесятеро») верно.
- Замечено, не находка: `hero_equity([], …)` отвечает `hero=0 вне диапазона игроков [0, -1]`,
  а не `за столом нет игроков` — как `icm._icm_branches`; из `analyze` недостижимо (герой
  всегда за столом).

**Итог ревью: НАХОДОК НЕТ.** Задача 3 закрыта. Чекпоинт §1.3: починок нет — реализация
Задачи 4 начата.

### Задача 4

Коммит `178ea65` — `feat(engine): analyze считает ICM по реальному полю и полной лесенке`.
Решения против наброска — блок «Решения при реализации (Фаза 3)» в тексте Задачи 4 (девять
пунктов). Кратко:

1. Файлов больше, чем в списке задачи: `handstate.py`, `test_handstate.py`, `ladder.py`
   (docstring), `icm_field.py` и `test_icm_field_pressure.py` (D4), `test_cli.py`, `README.md` (D2).
2. `TournamentContext.ladder()` — одно построение `PayoutLadder` для валидатора и `analyze`.
3. D17 — порядок по плану, шесть пинов.
4. D3 — ноль не доходит до расчёта, текст прежний, номер — `seatIndex`.
5. D4 — `icm_field.PressureUndefined(ValueError)`.
6. `нужен средний стек … и поле нечем населить` (было «остальное поле нечем схлопнуть»);
   отказ `стек меньше 0.1 BB не представим` из пути `analyze` ушёл вместе с `reduce_field`.
7. Плановый `test_hero_equity_is_a_real_share_of_the_prize_pool` не добавлен — дубль
   существующего; «глубокая лесенка» считается с полем 490.
8. Удалено 13 тестов старого поведения, каждый назван в сообщении коммита.
9. Самопроверка мутантами — ниже.

**Шаг 2** (6-max на старом `analyze`, `--tb=line`): отвалились 9 —
`test_hero_position_comes_from_the_button`, `…_broken_by_the_bigger_stack`,
`test_small_vpip_sample_…`, `test_hero_equity_belongs_to_the_hero_seat`,
`test_field_equal_to_the_table_…`, `test_hand_with_no_active_opponent_…`,
`test_a_final_table_is_analysed_without_the_average_stack`,
`test_all_in_opponent_…` (все — индексы и позиции 8-местного стола) и
`test_cli.py::test_analyze_on_an_all_in_opponent_is_a_json_error`. Каждый пересчитан под 6-max
или удалён (решение 8). Отклонение от шага 3: новые тесты писались вместе с реализацией, а не
до неё; что они краснеют, показали мутанты.

**Самопроверка мутантами** (не замена ревью Фазы 4). 27 мутантов `analyze.py`/`handstate.py`
(прогон `test_analyze.py`, `test_handstate.py`, `test_cli.py`, откат к копии в памяти) и 2
мутанта `icm_field.py`. Первый проход: 22 из 27 убиты, выжили пять — `chips <= 0 → < 0`,
стек поля = средний стек, герой и соперник переставлены в `risk_premium`, поле потеряно в
`risk_premium`, проход монотонности без сортировки. Добавлены три теста
(`test_the_answer_is_the_field_model_of_the_whole_tournament`,
`test_an_average_stack_that_leaves_the_field_no_chips_is_rejected`,
`test_payouts_listed_from_the_worst_place_are_accepted`), после них убиты все пять. Класс
«снести гард целиком»: гард стека, оба гарда `_field_stack`, `context.ladder()`, монотонность,
`entrants`, средний стек — убиты. `except PressureUndefined → except ValueError` убит;
`raise PressureUndefined → raise ValueError` в обеих функциях убит.

Приёмка Задачи 4, по пунктам:

1. `grep -rn "MAX_ICM_PREFIXES\|MAX_FIELD_NODES\|_icm_prefixes\|ladder_truncated\|reduced_field"
   src/poker_engine/analyze.py` — пусто. Во всём `src/` и `tests/` (кроме `field.py`,
   `icm.py` и их тестов) остались только два `not in` в
   `test_the_answer_no_longer_claims_a_truncated_ladder`.
2. `git diff 8fc0b4a..178ea65 --stat -- src/poker_engine/field.py tests/test_field.py` — пусто.
3. `test_a_final_table_has_no_field_and_is_not_flagged`.
4. `test_a_deeper_ladder_is_computed_instead_of_refused` — 165 мест, поле 490, считается,
   `ladder_incomplete` нет, `riskPremium` есть.
5. Каждое удаление названо в сообщении `178ea65`.
6. Сьют зелёный, см. ниже.

**Замер для Задачи 8** (снят после Задачи 4, до правок горячего пути): `analyze` на эталонной
6-max фикстуре — 7.9 с при `trials=10_000` и 1.8 с при `trials=2_000`, Linux-контейнер. ICM
здесь — доли секунды (поле 490 при 6 местах); остальное — Monte-Carlo эквити на `pokerkit`.

### Коммиты фазы

| Коммит | Что |
|---|---|
| `178ea65` | `feat(engine)`: Задача 4, `analyze` на модели поля; D2–D6, D17 |
| этот | `docs`: отчёт, реестр, промпт Фазы 4 |

### Тесты (D1)

До: 394 passed. После: **404 passed**, ни skip, ни xfail. Удалено 13 (десять в
`test_analyze.py`, три в `test_handstate.py` — поимённо в `178ea65`), добавлено 23: 11 в
`test_analyze.py`, 8 в `test_handstate.py`, 4 в `test_icm_field_pressure.py`. 394 − 13 + 23 = 404.
Полных прогонов за фазу — три.

### Долги

- **D2** — закрыт `178ea65`. `grep -n "reduced_field\|ladder_truncated\|MAX_ICM_PREFIXES\|свёрт\|схлоп"
  README.md` — только строка модуля `field` («свёртка поля (вне расчётного пути `analyze`)»);
  «Malmuth-Harville» остался только в строке модуля `icm` — это правда: `icm.py` остаётся эталоном.
- **D3** — часть Фазы 3 закрыта `178ea65`: `analyze` отвергает стек 0 BB до ICM текстом
  `стек на месте {seatIndex} должен быть > 0` (прежний текст и номер со скриншота; пины
  `test_all_in_opponent_is_rejected_before_the_icm` и CLI-тест). Дополнения Фаз 1–2 учтены:
  до `icm_field` ноль больше не доходит, поэтому ни номер позиции в списке, ни текст
  `эффективный стек равен нулю` из `analyze` недостижимы. Модель олл-ина на ноль (спека §11.1)
  не выбрана — хозяин финал, в ledger, решение за пользователем.
- **D4** — закрыт `178ea65`: `except PressureUndefined`. Контроль в Фазе 7 остаётся.
- **D5** — закрыт `178ea65`: `grep -rn MAX_FIELD_NODES src/` — пусто; предел узлов остался
  один — умолчание `field.reduce_field`, вне расчётного пути.
- **D6** — закрыт `178ea65`: `grep -rn "payout_ladder\b" src/ tests/` — только имя теста
  `test_empty_payout_ladder_is_rejected`, функции нет; `placesPaid ≤ 0`
  и глубина проверяются `PayoutLadder` (пины в `test_handstate.py`), сверка `placesPaid` с
  выплатами — `ladder_incomplete` (`test_a_ladder_shorter_than_places_paid_is_flagged`,
  `test_a_complete_ladder_is_not_flagged`).
- **D10** — проверен: скрипт на фикстуре с лесенкой на 165 мест, `node.playersLeft` 60, 496,
  520, 1107 и 1150 (больше `entrants` — вход, снятый после ре-энтри, пока контекст старый):
  сумма эквити стола и поля равна сумме призов за места `1..playersLeft` до `1e-6` во всех
  случаях; `analyze` отвечает без отказа. Инвариант 1 держится по построению: модель раздаёт
  места `1..t+F`, а `F` берётся из того же `playersLeft`. `node.playersLeft` с
  `context.entrants` валидатор не сверяет — и не должен: контекст снят раньше узла (см.
  docstring `analyze`). Итог — в ledger финала.
- **D17** — закрыт `178ea65`, см. решение 3.
- Новых долгов нет. Замечено, не долг: `hero_equity([], …)` называет диапазон `[0, -1]`.

### Где остановились

Фаза доделана целиком. Следующая — Фаза 4: ревью Задачи 4 (`178ea65`), затем Задача 5.

## Фаза 4 — ревью Задачи 4 и Задача 5 (2026-09-24)

Ветка `claude/serene-bell-yni6li` (облачная сессия, §3), Linux, `.venv/bin/python` (D16),
`.venv` был в контейнере. База: **404 passed** — совпадает с промптом (сумма по файлам
`--collect-only`, прогон без F/E).

### Ревью Задачи 4 (`178ea65`)

Объём: `analyze.py` целиком; `handstate.py` — `TournamentContext.ladder()`, `_validate_context`;
`ladder.py` — docstring и комментарий гарда глубины; `icm_field.PressureUndefined`; README;
`conftest.py`, `test_cli.py`, `test_icm_field_pressure.py`, новые и изменённые тесты
`test_analyze.py` и `test_handstate.py`; блок «Решения при реализации (Фаза 3)».

- **Спека.** §3.1: `F = players_left − t`, стек поля `(players_left × average − Σ table) / F` —
  `_field_stack` дословно; §3.3: `field_homogeneous` только при `F > 0`, `mh_bias` всегда;
  §4.4: `ladder_incomplete` = покрытых мест меньше `placesPaid` (`PayoutLadder.is_complete`,
  дыры между интервалами тоже считаются непокрытыми); §8.1: средний стек обязателен ровно при
  `players_left > мест за столом`; §8.3: `ladder_truncated`, `reduced_field` ушли, остальные
  пометки на месте. `F ≥ 0` гарантирован `handstate` (`осталось игроков … меньше, чем за столом`).
- **Global Constraints.** Один коммит на задачу; общих гардов не скопировано (`check_positive`
  из `_checks`); все новые отказы пинятся `match=` (один без `$` —
  `^стек на месте 2 должен быть > 0`, дальше идёт `, получено 0.0`, это верно).
- **Приёмка.** п.1 — `grep -c` по `analyze.py` для каждой из пяти строк: `0`, `0`, `0`, `0`,
  `0`; вне `field.py`/`icm.py` — только два `not in` в тесте и `.pyc`; п.2 —
  `git diff 8fc0b4a..178ea65 --stat -- field.py test_field.py` пусто; п.3 —
  `test_a_final_table_has_no_field_and_is_not_flagged`; п.4 —
  `test_a_deeper_ladder_is_computed_instead_of_refused` (165 мест, поле 490, `riskPremium`
  есть); п.5 — `git show 178ea65` по `^[-+]def test_`: в `test_analyze.py` 14 минусов =
  10 удалённых + 3 переименования + `test_a_final_table_is_analysed_without_the_average_stack`
  (переписан на месте); в `test_handstate.py` 3 минуса — все три `payout_ladder`; каждое
  удаление и переименование названо в сообщении; прирост `+11 +8 +4` (параметризация ×4) —
  394 − 13 + 23 = 404 сходится; п.6 — база 404.
- **Долги.** D2 — `grep "reduced_field|ladder_truncated|fieldNodes|payout_ladder|reduce_field|MAX_ICM_PREFIXES"`
  по `src/`, README, `.claude/skills/` — только `field.py`; D3 — отказ до ICM с `seatIndex`
  (`test_all_in_opponent_is_rejected_before_the_icm`, места перенумерованы, мутант «снести
  гард» даёт текст `icm_field` с позицией 2 и убит); D4 — `except PressureUndefined`, тесты
  подложенного отказа и четырёх ошибок вызова; D5 — `MAX_FIELD_NODES` в `src/` нет; D6 —
  `payout_ladder` нет, `placesPaid ≤ 0` и глубина — `PayoutLadder`; D17 — шесть пинов ровно по
  трём классам пар плана плюс сохранённая пара.
- **Мутационная приёмка** (19 мутантов, свои, не повтор списка Фазы 3; закоммиченный файл,
  откат к копии в памяти; `test_analyze.py` для `analyze.py`, `test_handstate.py` для
  `handstate.py`). Класс «снести гард целиком»: цикл `check_positive` стеков, `context.ladder()`
  в валидаторе, гард `average_stack_bb is None`, гард `chips <= 0` — убиты. Прочие: поле
  `−1`, `field_homogeneous` при `>= 0`, `ladder_incomplete` без `not`, `playersLeft` из
  контекста, `field_count < 0` вместо `<= 0`, стек поля делён на всех живых, фишки турнира
  по `context.players_left`, эквити героя без поля, `mh_bias` только при поле, пометка
  `icm_pressure_undefined` снесена, индекс героя = соперник, монотонность `>=`, монотонность
  без сортировки, `places_paid=players_left` в `ladder()`, интервал `(first, first)`,
  `entrants <= players_left` — **19 из 19 убиты**.

Находки:

- **F4.1** — `analyze.py:65–68` (docstring) и `README.md:69–71`: «призы за непокрытые места
  модель считает нулевыми, и `heroEquity` занижено» — неверно, когда непокрытые места глубже
  `playersLeft`: они уже вручены выбывшим и в расчёт не входят. Проверено: финальный стол
  фикстуры (`playersLeft=6`, выплаты 1–6), `placesPaid=165` против `placesPaid=6` —
  `heroEquity` одинаково `696.0069…`, а пометка стоит только в первом случае. Сама пометка
  верна по определению спеки §4.4 (сверка входа с лобби, ловит кривое распознавание и там,
  где на число не влияет), ложно только следствие. Почему это находка: правило плана «пометка
  ставится, только когда она правда» обесценивается, если объяснение пометки врёт.
  Чинить: сузить формулировку до «занижено, если среди непокрытых есть места не глубже
  `playersLeft`», и запинить тестом, что пометка ставится, а `heroEquity` не меняется, когда
  непокрытые места недостижимы.

Итог ревью: **одна находка, F4.1.**

### Починка

- **F4.1** — починена `573e12c` (`fix(engine): ladder_incomplete — про вход, а не про
  заниженное число`): docstring `analyze` и README говорят, что пометка сверяет вход с лобби,
  а `heroEquity` занижено, только если непокрытые места не глубже `playersLeft`. Семантика
  пометки (спека §4.4) не тронута. Пин —
  `test_an_incomplete_ladder_out_of_reach_is_flagged_but_costs_nothing`. Против текста плана
  ничего не решено, блока в плане нет.

Чекпоинт §1.3: одна находка, один коммит — реализация Задачи 5 начата. Задача 4 закрывается
ревью этой починки в Фазе 5.

### Задача 5

Коммит `37710bd` — `fix(engine): наличные за нокаут равны показанному ценнику, не его
половине` (сообщение по плану, хотя это задача, а не починка ревью). Решения против наброска —
блок «Решения при реализации (Фаза 4)» в тексте Задачи 5, четыре пункта: файлы сверх списка
(`test_cli.py`, `SKILL.md`); старую долю зашивали ещё четыре теста — переписаны; сверх плана —
инвариант §9.9 на пяти ценниках, `$4.87` в арифметике скриншота, пин гарда
`own_bounty_growth`, пин отказа `--split` в CLI; README — абзац про `--bounty` в «Командах»
(примера с `--split` там не было).

- **Шаг 2** — красный: `test_bounty.py` не собрался (`PROGRESSIVE_SHARE` нет), в CLI
  `0.25 != 0.2` и `--split` принимался.
- **Самопроверка мутантами** (не замена ревью Фазы 5): 7 мутантов `bounty.py`
  (прогон `test_bounty.py` + `test_cli.py -k bounty`) — наличные `× 0.5`,
  `PROGRESSIVE_SHARE = 1.0`, снос `_check_bounty` в `knockout_cash` и в `own_bounty_growth`,
  прирост без доли, фишки `× 0.5`, добавка в банк `× 0.5` — **7 из 7 убиты**.

Приёмка Задачи 5:

1. `test_cash_equals_the_price_shown_on_screen` — `knockout_cash(1.50) == 1.50`.
2. `test_the_bounty_pool_is_conserved` — арифметика `cash + (3.00 + 2 × growth) == 2 × 3.00`;
   плюс `test_a_knockout_moves_the_whole_bounty_of_the_busted` (инвариант §9.9).
3. `grep -rn "DEFAULT_SPLIT\|_check_split\|split=" src/ tests/` — пусто.
4. `grep -n "split" README.md` — только абзац о том, что флаг убран и что он теперь отвечает
   ошибкой. Буква приёмки «не упоминает `--split`» нарушена сознательно: план требует, чтобы
   ломающее изменение было видно в README, а без имени флага его не видно.
5. Сьют зелёный, 413 passed.

### Коммиты фазы

| Коммит | Что |
|---|---|
| `573e12c` | `fix(engine)`: F4.1, `ladder_incomplete` — про вход |
| `37710bd` | `fix(engine)`: Задача 5, модель половины, `--split` убран; D8 |
| этот | `docs`: отчёт, реестр, промпт Фазы 5 |

### Тесты (D1)

До: 404 passed. После: **413 passed**, ни skip, ни xfail. F4.1: +1 (404 → 405). Задача 5:
удалено 3 (`test_knockout_pays_half_by_default`, `test_knockout_split_is_configurable`,
`test_split_reaches_the_threshold` — названы в `37710bd`), добавлено 11: в `test_bounty.py`
десять (четыре плановых, `$4.87` внутри арифметики, инвариант §9.9 ×5, гард прироста), в
`test_cli.py` один. 405 − 3 + 11 = 413. Полных прогонов за фазу — два (база и после Задачи 5)
и один частичный (восемь первых файлов для подтверждения базы: `addopts = "-q"` в
`pyproject.toml` плюс `-q` в команде дают `-qq`, и итоговой строки с числом нет — число
базы взято из `--collect-only`, отсутствие падений — из точек и частичного прогона).

### Долги

- **D1** — числа выше.
- **D8** — закрыт `37710bd`: `SKILL.md` — соглашение о `--bounty` (показанный ценник =
  наличные, пополам не делить, `--split` нет), правило PKO дополнено приростом ценника
  ($1.50 → $2.25), ограничение «считается только наличная половина» заменено на «только
  наличные — показанный ценник соперника». Пример `bounty-ev` (`SKILL.md:37`) остался
  верным синтаксически, порог на нём теперь 0.2 (пин — `test_cli.py::test_bounty_command`).
- **D16** — `.venv/bin/python`.
- Новых долгов нет. Замечено: `own_bounty_growth` пока без вызовов в `src/` — его потребитель
  Задача 6 (Interfaces), не мёртвый код.

### Где остановились

Фаза доделана целиком. Следующая — Фаза 5: ревью Задачи 5 (`37710bd`) и починки F4.1
(`573e12c`), затем Задача 6.

## Фаза 5 — ревью Задачи 5 и Задача 6 (2026-09-24)

Ветка `claude/serene-bell-yni6li` (облачная сессия, §3), Linux, `.venv/bin/python` (D16),
`.venv` был в контейнере. База: **413 passed** за 233 с (`-o addopts="" -q`) — совпадает с
промптом.

### Ревью Задачи 5 (`37710bd`) и починки F4.1 (`573e12c`)

Объём: `git show 37710bd` целиком (`bounty.py`, `cli.py`, `test_bounty.py`, `test_cli.py`,
README, `SKILL.md`, блок решений в плане); `git show 573e12c` (docstring `analyze`, README,
тест); спека §2.4, §5, §8, §9.

- **Спека.** §5.2: `knockout_cash(B) = B`, `own_bounty_growth(B) = B/2` — дословно; проверка
  сохранением и арифметика экрана ($2.25, $3.00, $4.87 ≈ 4.875) — в тестах арифметикой, не
  числом. §5.5: `DEFAULT_SPLIT` ушёл, изменение CLI видно в README и тестах. §9.9 —
  `test_a_knockout_moves_the_whole_bounty_of_the_busted` на пяти ценниках, включая 0.
- **Global Constraints.** Один коммит на задачу. Новый гард — общий `_check_bounty` из
  модуля, копий нет. Новый отказ (`own_bounty_growth(-1)`) пинится `match=` с `^…$`.
- **Приёмка.** п.1 — `test_cash_equals_the_price_shown_on_screen`; п.2 —
  `test_the_bounty_pool_is_conserved`, арифметика `cash + (3.00 + 2 × growth) == 2 × 3.00`;
  п.3 — `grep -rn "DEFAULT_SPLIT\|_check_split\|split=" src/ tests/` пусто, `grep -i split`
  по `src/`, README, `.claude/skills/` — только `text.split(",")` в парсерах CLI и абзац
  README; п.4 — нарушен сознательно, довод Фазы 4 **принят**: сам план (абзац «`--split`
  убирается сознательно») и спека §5.5 требуют, чтобы ломающее изменение было видно в README,
  а без имени флага его не видно. Буква п.4 противоречит тексту той же задачи; побеждает
  требование спеки. Записано в план блоком решений Фазы 5. п.5 — база 413.
- **README и `SKILL.md` (D8).** Соглашение о `--bounty`, правило PKO ($1.50 → $2.25) и
  ограничение «считаются только наличные» согласованы с §5.2. Пример `bounty-ev` в таблице
  команд `SKILL.md` верен, его порог 0.2 пинит `test_bounty_command`.
- **F4.1 (`573e12c`).** Формулировка верна: эквити всех живых — сумма призов мест
  `1..playersLeft` (инвариант 1), место глубже `playersLeft` в расчёт не входит, поэтому
  нулевой приз там ничего не занижает; «только если» — необходимое условие, не достаточное,
  и так и сказано. Тест стоит на финальном столе (`run_analyze` ставит `playersLeft=6`):
  выплаты 1–6, `placesPaid=165` против `6` — пометка есть, `heroEquity` совпадает точно.
  Находок нет, **Задача 4 закрыта.**
- **Мутационная приёмка `bounty.py`** (17 мутантов, закоммиченный файл, откат к копии в
  памяти, прогон `test_bounty.py` + два теста `bounty-ev` в `test_cli.py`). Класс «снести гард
  целиком»: `_check_bounty` в `knockout_cash`, в `own_bounty_growth`, в
  `required_equity_with_bounty`; `_check_chip_value` в `bounty_in_chips`; `_check_villain_stack`;
  `check_non_negative(pot_before_call)` — убиты. Прочие: `PROGRESSIVE_SHARE` 1.0 и 0.25,
  наличные `× PROGRESSIVE_SHARE`, прирост без доли, фишки `× chip_value`, `covers` через `>` и
  через `<=`, добавка в колл вместо банка — убиты. **Выжили три:**
  - снос `check_positive(call_amount)` — эквивалентный: при `call_amount ≤ 0` покрытие
    невозможно (`villain_stack > 0` проверен раньше), и `potodds.required_equity` бросает тот
    же текст тем же первым гардом;
  - `if not covers_villain:` без `bounty > 0` — эквивалентный: при нулевом ценнике добавка 0;
  - снос `_check_chip_value` в `required_equity_with_bounty` — **не** эквивалентный: на пути
    «соперник не покрыт» `chip_value=0` проходит молча, а docstring обещает валидацию всех
    параметров до `covers_villain`. Существует с плана 2, но ловит его обязательная мутационная
    приёмка этого модуля, а Задача 6 делает функцию потребителем `analyze`.

Находки:

- **F5.1** — `tests/test_bounty.py:113–186`: (а) гард `_check_chip_value` в
  `required_equity_with_bounty` не пинится ни одним тестом на пути без покрытия — мутант «снести
  гард» выживает; (б) пять тестов отказа (`test_rejects_nonpositive_chip_value`,
  `…_negative_bounty_even_when_villain_not_covered`, `…_nonpositive_villain_stack`,
  `…_negative_pot_even_when_bounty_applies`, `…_nonpositive_call_amount`) — голый
  `pytest.raises(ValueError)` в модуле с шестью гардами, против конвенции `CLAUDE.md`. Оба
  пункта — наследие планов 1–2, Задача 5 правила этот файл и их не заметила. Чинить: тест
  `chip_value=0` при непокрытом сопернике с `match=`, и `match=` с `^…$` у пяти голых отказов.

Итог ревью: **одна находка, F5.1.**

### Починка

- **F5.1** — починена `8616497` (`fix(engine): отказы bounty пинят текст, гард цены фишки —
  и без покрытия`): `test_rejects_nonpositive_chip_value_even_when_villain_not_covered`, `match=`
  с `^…$` у пяти голых отказов. Тексты гардов не менялись. Повторный прогон класса «снести гард»
  (8 мутантов) — выжил только эквивалентный `check_positive(call_amount)`. Решение записано в
  план блоком «Решения по находкам ревью (Фаза 5)» в Задаче 5 вместе с оценкой п.4 приёмки.

Чекпоинт §1.3: одна находка, один коммит — реализация Задачи 6 начата. Задача 5 закрывается
ревью этой починки в Фазе 6.

### Задача 6

Коммит `61e57fd` — `feat(engine): PKO в разборе — ценники голов со стола`. Решения против
наброска — блок «Решения при реализации (Фаза 5)» в тексте Задачи 6, семь пунктов. Главные:

1. `pko` ставится по наличию ценников (признак турнира, §5.1), блок `bounty` — при сопернике.
2. Ценник обязателен у героя (текст плана) **и у каждого в раздаче** — иначе в PKO наружу
   уходил порог без головы. Валидация — `handstate._validate_prices`, до ICM.
3. `bbValueUsd` вместо `chipValueUsd` (единица — большой блайнд); числитель — призы мест
   `1..min(playersLeft, placesPaid)`, а не `ladder.total()`: на ITM вручённые места завышали бы
   цену блайнда. Знаменатель — стол плюс поле модели ICM. Нулевые деньги в игре — ключей нет.
4. Голова в пороге — только при возможном нокауте: колл накрывает остаток соперника (правило
   `bounty.py`) и герой с вложенным покрывает соперника с вложенным.
5. Плановый тест «голова снижает порог» на фикстуре был бы красным: остаток соперника 23.2 BB
   больше колла 7.3, нокаута нет. Переписан на остаток 5.0 с аналитическим порогом. Колл
   против настоящего олл-ина (остаток 0) до блока не доходит — D3.
6. README — сверх списка файлов: список пометок, вход `bountyUsd`, блок `bounty`.

- **Шаг 2** — красный: 22 падения (`AttributeError`/`KeyError: 'bounty'`/`DID NOT RAISE`),
  5 зелёных — классические тесты, которым и положено проходить.
- **Самопроверка мутантами** (не замена ревью Фазы 6): 12 по `analyze.py` (`pko` по `all`,
  блок без ценников, сумма всей лесенки, `in_play = places_paid`, фишки = стол, снос гарда
  нулевых денег, `can_bust` всегда, `can_bust` без вложенного соперника, снос `to_call > 0`,
  наличные = прирост, риск = ценник соперника, стек соперника = вложенное) и 7 по
  `handstate.py` (снос гарда отрицательного ценника, раннего выхода, гарда героя, гарда в
  раздаче, `in_hand` из условия, вызова `_validate_prices`, разбора `bountyUsd`) — **19 из 19
  убиты**. Два «убиты» через error проверены: блок без ценников валит модульную фикстуру
  классики (`TypeError` на `None`) — поведение; снос тела цикла без `pass` был синтаксической
  ошибкой, перезапущен с `pass` — убит тестом.

Приёмка Задачи 6:

1. `test_a_table_without_prices_is_not_a_pko` (ни `pko`, ни `bounty`) и
   `test_a_classic_table_never_touches_the_bounty_module` (три функции `bounty.py` подменены
   на `AssertionError`).
2. `test_a_price_on_the_villain_but_not_on_the_hero_is_rejected` и
   `test_handstate.py::test_a_price_on_a_rival_without_one_on_the_hero_is_rejected` — полный
   текст с `^…$`.
3. `test_the_two_halves_of_equity_stay_separate` — `icm` не меняется при удвоенных ценниках,
   блок `bounty` отдельный ключ, ничего не сложено.
4. `grep -rn "0\.5\|половин" src/poker_engine/analyze.py src/poker_engine/handstate.py` — доли
   фонда нет; единственная доля — `PROGRESSIVE_SHARE` в `bounty.py` (прирост ценника, не доля
   фонда).
5. Сьют зелёный, 439 passed.

### Коммиты фазы

| Коммит | Что |
|---|---|
| `8616497` | `fix(engine)`: F5.1, отказы `bounty` пинят текст |
| `61e57fd` | `feat(engine)`: Задача 6, PKO в разборе |
| этот | `docs`: отчёт, реестр, промпт Фазы 6 |

### Тесты (D1)

До: 413 passed. После: **439 passed**, ни skip, ни xfail, удалений нет. F5.1: +1 (413 → 414).
Задача 6: +16 в `test_analyze.py`, +9 в `test_handstate.py`. 414 + 25 = 439. Полных прогонов
за фазу — два (база 233 с, после Задачи 6 265 с).

### Долги

- **D1** — числа выше.
- **D3** — дополнен: стек 0 у соперника отвергается до ICM, поэтому колл против олл-ина — главный
  спот PKO — не получает `requiredEquityWithBounty`; порог с головой сейчас бывает только при
  остатке соперника меньше колла. Решение о модели — за пользователем, в ledger финала.
- **D16** — `.venv/bin/python`.
- Новых долгов с хозяином нет. Замечено для Фазы 7 (бюджет): `_bounty_block` суммирует
  `ladder.prize` по `min(playersLeft, placesPaid)` местам — O(мест) двоичных поисков на разбор,
  в PKO; на 165 местах это микросекунды.

### Где остановились

Фаза доделана целиком. Следующая — Фаза 6: ревью Задачи 6 (`61e57fd`) и починки F5.1
(`8616497`), затем Задача 7 (D9 — вопрос пользователю до реализации).

## Фаза 6 — ревью Задачи 6 и Задача 7 (2026-09-24)

Ветка `claude/serene-bell-yni6li` (облачная сессия, §3), Linux, `.venv/bin/python` (D16),
`.venv` был в контейнере. База: **439 passed** за 238 с (`-o addopts="" -q`) — совпадает с
промптом.

### Ревью Задачи 6 (`61e57fd`) и починки F5.1 (`8616497`)

Объём: `git show 61e57fd` целиком (`handstate.py`, `analyze.py`, `test_analyze.py`,
`test_handstate.py`, README); `git show 8616497` (`test_bounty.py`); текст Задачи 6 с блоком
решений Фазы 5; спека §5, §8.1–§8.3, §9 (инварианты 9–11).

- **Спека.** §5.1 — признак PKO по ценникам, без настройки: `is_pko` по `bounty_usd`. §5.2 —
  наличные = ценник, прирост = половина, через `bounty.py`. §5.3 — взятых баунти в контракте
  нет, в решении ровно ценник соперника и ценник героя. §8.1 — ценник героя обязателен (плюс
  решение 3). §8.2 — блок `bounty` со всеми пятью величинами, отдельно от `icm`. §8.3 — `pko`.
  Инвариант 11 — монки-патч трёх функций `bounty.py`.
- **Global Constraints.** Один коммит. Новых общих гардов нет, `check_non_negative` из
  `_checks.py`; имя `ценник на месте N` — мужской род. Тесты отказов пинят текст (`^…$` в
  `test_handstate.py`), два пина порядка отказов. Пометка `pko` правдива и без соперника.
- **Решения Фазы 5 — оценка каждого:**
  1. README сверх списка — **принято**: без него README врал бы о пометках и входе (как D2).
  2. `pko` по ценникам, не в ветке соперника — **принято**: пометка про турнир (§5.1), блок —
     про раздачу; `test_a_pko_without_a_villain_is_still_a_pko`.
  3. Ценник обязателен у всех в раздаче — **принято**. Строже буквы §8.1 («у героя»), но буква
     оставляла путь «порог без головы в PKO» — ровно ошибку §2.4, причём молча. Отказ лучше
     правдоподобного неверного числа. Цена — мультивей, где у третьего в раздаче ценник не
     распознан, отвергается целиком; это видно по тексту отказа, а не молча.
  4. `bbValueUsd` из призов `1..min(playersLeft, placesPaid)` — **принято**: на ITM
     `ladder.total()` включал бы вручённые места (тест с лесенкой на 165 мест — в разы). `min`
     — граница цикла, не смысл: за призовой зоной `prize` платит 0, мутант `in_play =
     players_left` **эквивалентен** по значению; `min` держит цикл в `placesPaid` итерациях при
     тысячах живых (бюджет, Задача 8).
  5. «Нокаут возможен» — **принято**. `villain_stack` в `analyze` — остаток за спиной, и правило
     `bounty.py` «колл накрывает остаток» на остатке > 0 — эвристика «соперник привязан к
     банку», а не олл-ин; настоящий олл-ин (остаток 0) закрыт D3. Условие покрытия с вложенным
     верно: на прошлых улицах оба в раздаче вложили поровну, значит сравнивать надо стеки на
     начало улицы. Но граница условия тестами не пинится — см. F6.1.
  6. Плановый тест «голова снижает порог» переписан — **принято**, проверено: остаток соперника
     23.2 > колла 7.3, `covers_villain` ложно, порог равен обычному, и `<` был бы красным;
     этот случай пинит `test_no_credit_for_a_head_the_call_does_not_take`.
  7. Тесты сверх плана — **принято**.
- **Приёмка Задачи 6.** п.1 — `test_a_table_without_prices_is_not_a_pko` и
  `test_a_classic_table_never_touches_the_bounty_module`; п.2 —
  `test_a_price_on_a_rival_without_one_on_the_hero_is_rejected` (`^…$`); п.3 —
  `test_the_two_halves_of_equity_stay_separate`; п.4 — `grep -n "0\.5\|половин"` по `analyze.py`
  и `handstate.py` пусто; п.5 — база 439.
- **F5.1 (`8616497`).** Новый тест пинит `_check_chip_value` на пути без покрытия текстом
  `^…$`; у пяти отказов `match=` с полным текстом; голых `pytest.raises(ValueError)` в
  `test_bounty.py`, `test_analyze.py`, `test_handstate.py` нет (grep). Находок нет, **Задача 5
  закрыта.**
- **Мутационная приёмка** (25 мутантов, закоммиченные файлы, откат из копии в памяти,
  `test_analyze.py -k` по PKO-тестам, `test_handstate.py -k price…`). Класс «снести гард
  целиком»: гард отрицательного ценника, ранний выход, гард героя, гард в раздаче, вызов
  `_validate_prices`, гард нулевых денег — убиты. Прочие: `pko` по `all`, снос пометки, блок
  всегда, `in_play = placesPaid`, `< 0`, `can_bust = True`, без вложенного соперника,
  `to_call >= 0`, фишки = стол, `villain_stack = invested`, наличные = прирост, риск = ценник
  соперника, цена соперника = ценник героя, `range(1, in_play)`, `in_hand` из условия, разбор
  `bountyUsd` — убиты. **Выжили три:** `in_play = players_left` — эквивалентный (выше);
  `can_bust` через `>` и `can_bust` без вложенного героя — **не** эквивалентные.

Находки:

- **F6.1** — `tests/test_analyze.py` (блок PKO): граница условия `can_bust`
  (`analyze.py`, `_bounty_block`) не пинится. Мутанты `>=` → `>` (равные стеки: герой выбивает,
  голова не засчитана бы) и «без `hero.invested_bb`» (герой, накрывающий соперника только с
  вложенным, терял бы голову) выживают. Чинить: тест на ровном покрытии — стек героя 11.3 +
  вложенные 1.0 = 12.3 = 5.0 + 7.3 у соперника (в float равенство точное), голова засчитана,
  порог аналитический. Один тест убивает оба мутанта.

Итог ревью: **одна находка, F6.1.**

### Починка

- **F6.1** — починена `b15f8e8` (`fix(engine): граница нокаута в PKO — ровное покрытие с
  вложенным пинится`): `test_a_hero_covering_the_villain_exactly_with_invested_gets_the_head`.
  Код не менялся. Повторный прогон трёх мутантов `can_bust` на новом тесте: `>` и «без
  вложенного героя» — убиты; `can_bust = True` этим тестом не убивается (и не должен — его
  убивают два теста «без головы»). Записано в план блоком «Решения по находкам ревью (Фаза 6)»
  в Задаче 6.

Чекпоинт §1.3: одна находка, один коммит — реализация Задачи 7 начата. Задача 6 закрывается
ревью этой починки в Фазе 7.

### D9 — вопрос пользователю до реализации

Задан до Задачи 7 (спека §11.2), ответ 2026-09-24:

- **кто ранняя пташка** — «все, по плану»: возврат получает любой, кто вылетел вне денег;
  лесенка удлиняется одним интервалом для всех (кто из живых пташка, движок не знает);
- **где кончается бабл** — «все места за деньгами»: места `placesPaid + 1 .. playersLeft`,
  всегда, когда живых больше оплачиваемых; `lateRegOpen` не влияет.

Это ровно прочтение плана. **D9 закрыт** (`03aa820`): допущение в docstring
`TournamentContext.protected_ladder` и в README. Открытым вопросом в ответе `analyze` не
оставлено — оно подтверждено.

### Задача 7

Коммит `03aa820` — `feat(engine): поправка на компенсацию бай-ина при вылете на баббле`.
Решения против наброска — блок «Решения при реализации (Фаза 6)» в тексте Задачи 7, семь
пунктов. Главные:

1. Удлинение только при возврате > 0 и `playersLeft > placesPaid`: набросок на ITM падал бы в
   `PayoutLadder` на интервале `7–6`. Поправки нет — нет ни пометки, ни блока, ответ тот же.
2. Удлинённую лесенку строит `TournamentContext.protected_ladder`; `_intervals()` — общий с
   `ladder()`.
3. `riskPremiumWithoutRefund` — словарь той же формы, что `riskPremium`; тесты наброска
   сравнивали словарь с числом. Общий `_pressure` для обеих лесенок.
4. `ladder_incomplete` и `bbValueUsd` — по лесенке лобби (`prizes`): возврат платит рум, не фонд.
5. Возврат больше минимального приза не отвергается; давление с ним может быть не определено —
   пометка, давление без возврата в блоке остаётся.

- **Шаг 2** — красный: 8 падений (`AttributeError`, пометки нет, `DID NOT RAISE`), 5 зелёных —
  тесты «ничего не меняется» (умолчание, ITM, ноль, полная лесенка, цена блайнда), им и
  положено проходить без кода.
- **Аналитический тест сверх плана:** возврат c на местах вне денег ≡ «все оплачиваемые призы
  на c ниже» плюс c на каждом месте; каждый игрок где-то финиширует, поэтому эквити героя
  сдвигается ровно на c, а риск-премия и bubble factor (разности эквити) не меняются. Заодно
  видно, почему поправка смягчает давление: ступень «вылет → минимальный приз» ниже на c. На
  фикстуре (места 1–6, 9 живых, $6.60) риск-премия 0.05654 → 0.05622, bubble factor
  1.2550 → 1.2534.
- **Самопроверка мутантами** (не замена ревью Фазы 7): 15 — снос удлинения, снос пометки,
  `ladder_incomplete` по удлинённой, давление без возврата по удлинённой, цена блайнда по
  удлинённой, снос `try` вокруг давления без возврата, снос блока давления без возврата,
  `places.from + 2`, снос гарда возврата, ноль не пропускается, граница `<`, начало интервала
  `+2`, конец `playersLeft − 1`, разбор выключен, возврат пополам. **14 убиты**, выжил
  `ladder_incomplete` по удлинённой — **эквивалентный** (интервал возврата покрывает ровно
  добавленные места; записано в комментарии `analyze.py`).

Приёмка Задачи 7:

1. `test_bubble_protection_is_absent_by_default` (ни пометки, ни блока на базовом ответе); числа
   без поля не тронуты — прежние тесты `test_analyze.py` зелёные без правок; ноль и ITM дают
   ответ, равный ответу без поля (`==` целиком).
2. `test_a_refund_softens_the_ladder_pressure` — сравнение, риск-премия и bubble factor.
3. Допущение — docstring `TournamentContext.protected_ladder` (и абзац `analyze`).
4. `test_a_refund_on_a_complete_ladder_is_not_an_incomplete_one` и
   `test_the_refund_does_not_fake_a_complete_ladder` (дыра 7–165 остаётся видна).
5. Сьют зелёный, 459 passed.

### Коммиты фазы

| Коммит | Что |
|---|---|
| `b15f8e8` | `fix(engine)`: F6.1, граница нокаута в PKO |
| `03aa820` | `feat(engine)`: Задача 7, защита на баббле |
| этот | `docs`: отчёт, реестр, промпт Фазы 7 |

### Тесты (D1)

До: 439 passed. После: **459 passed**, ни skip, ни xfail, удалений нет. F6.1: +1 (439 → 440).
Задача 7: +14 в `test_analyze.py`, +5 в `test_handstate.py`. 440 + 19 = 459. Полных прогонов
за фазу — два (база 238 с, после Задачи 7 296 с).

### Долги

- **D9** — закрыт ответом пользователя, `03aa820`.
- **D1** — числа выше.
- **D16** — `.venv/bin/python`.
- **D20 (новый)** — при защите на баббле `analyze` считает давление второй раз, по лесенке лобби
  (`riskPremiumWithoutRefund`): ещё один расчёт давления — сейчас до шести вызовов
  `table_equities`, после шага 6 Задачи 8 три — сверх тех, что Задача 8 сводит в три. Приёмка п.3 Задачи 8 («не больше трёх за разбор») обязана оговорить этот случай
  (три на лесенку — шесть с защитой), а `_pressure` в `analyze.py` — заменить на
  `icm_field.pressure` шага 6. Хозяин — Фаза 7, тем же коммитом Задачи 8.

### Где остановились

Фаза доделана целиком. Следующая — Фаза 7: ревью Задачи 7 (`03aa820`) и починки F6.1
(`b15f8e8`), затем Задача 8 (бюджет двух секунд, D7, D20; нужна сеть для `eval7`).

## Фаза 7 — ревью Задачи 7 и Задача 8 (2026-09-25)

Ветка `claude/serene-bell-yni6li` (облачная сессия, §3), Linux, `.venv/bin/python` (D16).
`.venv` в контейнере не было — создан по `CLAUDE.md` (Python 3.11.15). База: **459 passed** за
222 с (`-o addopts="" -q`) — совпадает с промптом.

### Ревью Задачи 7 (`03aa820`) и починки F6.1 (`b15f8e8`)

Объём: `git show 03aa820` целиком (`handstate.py`, `analyze.py`, `test_analyze.py`,
`test_handstate.py`, README); `git show b15f8e8` (`test_analyze.py`); текст Задачи 7 с блоком
решений Фазы 6; спека §6, §8.1–§8.3, §9 (инвариант про возврат `c`), `ladder.py`, `_checks.py`.

- **Спека.** §6 — необязательное поле контекста (`bubbleRefundUsd`), поправка видна отдельной
  строкой (`bubbleProtection` с давлением без возврата), а не растворена. §8.3 —
  `bubble_protection`. Инвариант §9 «возврат `c` сдвигает эквити на `c` и не меняет риск-премию
  и bubble factor» пинит `test_a_refund_is_the_same_as_every_paid_prize_lowered_by_it` — на бумаге
  верно: разница лесенок — константа `c` на местах `1..playersLeft`, а вероятности мест героя
  в сумме 1.
- **Global Constraints.** Один коммит. Гард — `check_non_negative` из `_checks.py`, имя
  `возврат бай-ина` — мужской род; NaN и `inf` отсекает общий `_check_number`/`_check_finite`.
  Отказы пинят текст (`^…$`). Пометка правдива: ставится ровно тогда, когда лесенка удлинена.
- **Решения Фазы 6 — оценка каждого:**
  1. D9 по ответу пользователя — **принято**, сверено: docstring
     `TournamentContext.protected_ladder` говорит все три части ответа (каждый вылетевший вне
     денег; места `placesPaid+1..playersLeft`; регистрация не важна). **README говорит две из
     трёх** — см. F7.1.
  2. Удлинение только при возврате > 0 и `playersLeft > placesPaid` — **принято**. Иначе
     `PayoutLadder` падает на интервале `7–6` (ITM) или на призе 0 (`check_positive`); по смыслу
     на ITM вылета вне денег нет. Граница пинится `test_no_protection_when_everyone_left_is_paid_exactly`
     (мутант `<` убит), ноль — `test_a_zero_refund_changes_nothing` (мутант `is None` убит).
  3. Лесенку строит `handstate` — **принято**: `_intervals()` общий, копии нет.
  4. Форма блока (`riskPremiumWithoutRefund` — словарь формы `riskPremium`) — **принято**:
     набросок сравнивал словарь с числом; одна форма у обоих давлений читается однозначно.
  5. `ladder_incomplete` и `bbValueUsd` по лесенке лобби — **принято**. Для пометки эквивалентно
     (проверено: удлинённая покрывает `covered + (playersLeft − placesPaid)` из `playersLeft`,
     полна ровно когда полна лобби). Для цены блайнда — по смыслу: возврат достаётся любому
     вылетевшему на баббле вне зависимости от фишек, цена фишки в долларах от него не растёт;
     пинит `test_the_refund_does_not_enter_the_price_of_a_blind` (мутант убит).
  6. Возврат больше минимального приза не отвергается — **принято**: противоречия в данных
     нет, давление с ним правда не определено, и пометка `icm_pressure_undefined` — правда.
  7. Сверх списка файлов (README, `test_handstate.py`) и тесты сверх плана — **принято**.
- **Приёмка Задачи 7.** п.1 — `test_bubble_protection_is_absent_by_default` и сравнения `==`
  целиком (ITM, ноль, граница); п.2 — `test_a_refund_softens_the_ladder_pressure` (сравнение,
  обе величины); п.3 — docstring `protected_ladder`; п.4 —
  `test_a_refund_on_a_complete_ladder_is_not_an_incomplete_one`,
  `test_the_refund_does_not_fake_a_complete_ladder`; п.5 — база 459.
- **F6.1 (`b15f8e8`).** Числа теста проверены: 11.3 + 1.0 == 5.0 + 7.3 в float точно; порог
  `7.3 / (9.4 + extra + 7.3)` — формула `required_equity_with_bounty` с головой. Мутанты
  `>` и «без вложенного героя» убиты (ниже). Находок нет, **Задача 6 закрыта.**
- **Мутационная приёмка** (20 мутантов, скрипт: замена строки в закоммиченном файле,
  `pytest -x --tb=line -k refund|bubble|protection|covering|head`, откат из копии в памяти).
  Класс «снести гард целиком»: снос `check_non_negative` возврата, снос пометки, снос блока,
  снос давления без возврата — убиты. Прочие: `is None` вместо `not refund`, `<` на границе,
  начало интервала `+2`, конец `playersLeft − 1`, разбор выключен, `return self.ladder()`
  вместо `None`, `ladder = prizes`, `from = placesPaid`, `refundUsd = 0`, давление без возврата
  по удлинённой, `raise` вместо `pass`, цена блайнда по удлинённой, F6.1 `>` и без вложенного
  героя — убиты. **Выжили два, оба эквивалентные:** `not ladder.is_complete` (известный,
  комментарий в `analyze.py`) и `places_paid=players_left + 1` у удлинённой лесенки — её
  `places_paid` в `analyze` не читается никем: `icm_field` зовёт только `prize()`, пометка и цена
  блайнда берут лесенку лобби.

Находки:

- **F7.1** — `packages/poker-engine/README.md`, абзац «Защита на баббле»: из ответа пользователя
  по D9 (журнал Фазы 6) README передаёт «каждый вылетевший вне денег» и места, но не «открыта
  ли регистрация, не важно». Спека §6 цитирует экран GG «заканчивается после завершения
  регистрации», и читатель README вправе ждать связи с `lateRegOpen` — её нет. Docstring
  `protected_ladder` это говорит, README — нет. Чинить: дописать в абзац, что `lateRegOpen` на
  поправку не влияет.

Итог ревью: **одна находка, F7.1.**

### Починка

- **F7.1** — починена `a42dfee` (`fix(engine): README — защита на баббле не зависит от
  регистрации`): в абзац README дописано, что `lateRegOpen` на поправку не влияет. Код не
  менялся; против текста плана решений нет, блок в Задачу 7 не нужен.

Чекпоинт §1.3: одна находка, один коммит — реализация Задачи 8 начата. Задача 7 закрывается
ревью этой починки в Фазе 8.

### Замеры «до» и «после» (эталонная фикстура `conftest`, `analyze` с `trials=10_000`)

Сняты скриптом: `analyze(_context(), [_node()])` трижды, счётчик и таймер на
`icm_field.table_equities` и на `equity_vs_range`. Linux, Python 3.11.15, эта облачная машина.

| | Всего | ICM (`table_equities`) | Сила руки |
|---|---|---|---|
| до (`6487807`+`a42dfee`) | 6.82–6.91 с | 7 вызовов, 0.12–0.17 с | 6.65–6.79 с |
| после (`59ac718`) | 0.115–0.123 с | 3 вызова, 0.047–0.048 с | 0.067–0.074 с |

Полный сьют: 222 с до, ~7 с после.

### Задача 8

Коммит `59ac718` — `perf(engine): eval7 в горячем пути и три расчёта ICM вместо семи`.
Решения против наброска — блок «Решения при реализации (Фаза 7)» в тексте Задачи 8, семь
пунктов. Главные:

1. `pressure` — как в наброске; эквити героя при сопернике берётся из него. При неопределённом
   давлении эквити стоит ещё одного вызова `hero_equity`.
2. Приёмка п.3 оговорена: `≤ 3` на фикстуре, `1` без соперника, `≤ 6` с защитой (D20), `≤ 7` с
   защитой при неопределённом давлении — каждый случай свой тест.
3. Два теста `test_analyze.py` подменяют `pressure` вместо `risk_premium` (утверждения те же).
4. В лок записаны `eval7` и его зависимости `future`, `pyparsing`. **Колёс `eval7 0.1.11` для
   Windows на Python 3.13+ нет, sdist нет** — новый долг D21.
5. Фильтр `DeprecationWarning` от `eval7.rangestring` в `pyproject.toml`.

- **Шаг 3** — тест согласия зелёный до правок: 3000 пар без расхождений; сверх наброска —
  3000 пар с общей доской, 125 ничьих, расхождений нет.
- **Шаг 5** — `test_equity.py`, `test_equity_crosscheck.py`, `test_equity_vs_range.py`: 23
  passed без правок.
- **Красный на медленной реализации проверен:** с `equity.py` из HEAD тест бюджета падает
  («разбор занял 6.85 с»); с `analyze.py` из HEAD счётчики падают (7, 13, 13 вызовов).
- **Самопроверка мутантами** (не замена ревью Фазы 8): 11 — перестановка риск-премии и bubble
  factor в `pressure`, `win` вместо `now`, `hero_equity` всегда, перестановка ключей в двух
  блоках давления, `except ValueError` (D4), давление без возврата по удлинённой, эквити при
  неопределённом давлении по лесенке лобби, `min` вместо `max` в оценщике, ничьи без деления,
  доска без последней карты. Первый прогон дал **выжившего**: эквити при неопределённом давлении
  по лесенке лобби — добавлен тест
  `test_undefined_pressure_keeps_the_equity_on_the_protected_ladder`, мутант убит. Плюс два
  `raise` вместо `pass` вокруг обоих `pressure` — убиты.

**Урок для мутантов (важно для Фазы 8).** Мутант той же длины, что исходная строка
(`return win,` вместо `return now,`, `+ 2` вместо `+ 1`), откатанный в ту же секунду,
оставляет в `__pycache__` скомпилированный мутант: Python сверяет `.pyc` по mtime в секундах и
размеру исходника. Следующие прогоны — и мутанты в *других* файлах, и обычный сьют — идут на
мутанте. Здесь это вскрылось падением полного сьюта после мутант-прогона; после чистки кэша
перепрогнаны оба набора (Задачи 7 и 8) — вердикты выше уже с чистого кэша. Скрипт мутантов
обязан ставить `PYTHONDONTWRITEBYTECODE=1` и чистить `src/**/__pycache__` перед каждым
прогоном. Мутант-прогоны Фаз 1–6 шли тем же способом отката — их вердикты «убит» могли быть
получены на чужом мутанте (долг D22).

Приёмка Задачи 8:

1. `test_the_fast_evaluator_orders_hands_exactly_like_pokerkit` — 3000 пар, `disagreements == []`.
2. `test_equity_crosscheck.py` без правок (`git show 59ac718 --stat` его не содержит).
3. `test_the_icm_half_of_the_budget_is_spent_three_times_not_seven` и три оговорённых случая.
4. `test_a_full_analysis_fits_the_budget` — 0.12 с при бюджете 2 с.
5. `eval7>=0.1.11` в `pyproject.toml`, `eval7==0.1.11` в `requirements-dev.lock`.
6. `pokerkit>=0.5` в `dependencies` остался; в `src` он больше не импортируется, только в
   `test_equity_evaluator.py`.
7. 469 passed.

### Коммиты фазы

| Коммит | Что |
|---|---|
| `a42dfee` | `fix(engine)`: F7.1, README — регистрация не влияет на защиту |
| `59ac718` | `perf(engine)`: Задача 8, eval7 и три вызова ICM |
| этот | `docs`: отчёт, реестр, промпт Фазы 8 |

### Тесты (D1)

До: 459 passed. После: **469 passed**, ни skip, ни xfail, удалений нет. Задача 8: +2
`test_equity_evaluator.py`, +5 `test_analyze_budget.py`, +2 `test_icm_field_pressure.py`,
+1 `test_analyze.py`. 459 + 10 = 469. Полных прогонов за фазу — четыре (база 222 с; после
Задачи 8 три по ~7 с, один из них упал на устаревшем `.pyc` мутанта — см. урок). Лимит §1.3
«не больше трёх» писан под 5.5 минуты на прогон; при 7 с он теряет смысл.

### Долги

- **D7** — закрыт, `59ac718`: `table_equities` 7 → 3, счётчик в тесте.
- **D20** — закрыт, `59ac718`: `_pressure` удалён, давление без возврата — `icm_field.pressure`,
  приёмка п.3 оговорена (`≤ 6` с защитой).
- **D4 (контроль)** — `except` в `analyze` ловит только `PressureUndefined` (мутант
  `except ValueError` убит `test_only_undefined_pressure_is_turned_into_a_flag`, тест зелёный).
- **D1** — числа выше. **D16** — `.venv/bin/python`; `.venv` создан.
- **D21 (новый)** — у `eval7 0.1.11` нет колёс для Windows на Python 3.13+ и нет sdist, а лок
  писан на Python 3.14.4 Windows: на машине пользователя `pip install -r requirements-dev.lock`
  упадёт. Варианты: `.venv` от Python 3.12 на Windows (README это советует); другой оценщик с
  колёсами под 3.14; сборка `eval7` из git с компилятором. Решение — за пользователем. Хозяин —
  Фаза 8 (оценить, спросить пользователя), итог — в ledger финала.
- **D22 (новый)** — вердикты мутант-прогонов Фаз 1–6 могли быть получены на устаревшем `.pyc`
  чужого мутанта той же длины (урок выше). Хозяин — Фаза 8: в финальном ревью ветки
  перепрогнать мутанты-«гарды» с `PYTHONDONTWRITEBYTECODE=1` (при сьюте в 7 с это дёшево).

### Где остановились

Фаза доделана целиком. Следующая — Фаза 8: ревью Задачи 8 (`59ac718`) и починки F7.1
(`a42dfee`), финальное ревью ветки, D12, D21, D22.

## Фаза 8 — ревью Задачи 8 и финальное ревью ветки (2026-09-25)

Ветка `claude/serene-bell-yni6li` (облачная сессия, §3), Linux, `.venv/bin/python` (D16),
Python 3.11.15. База: **469 passed** за 7.9 с (`-o addopts="" -q`), совпадает с промптом.
Фаза только ревьюит, код не менялся.

### Ревью Задачи 8 (`59ac718`) и починки F7.1 (`a42dfee`)

Объём: `git show 59ac718` целиком, Задача 8 плана с блоком решений Фазы 7, спека §7 и §9.12,
`icm_field.py` и `analyze.py` в HEAD.

- **Спека §7.** §7.2: три вызова ICM на разбор. `pressure` считает три ветви один раз;
  `_equity_after` при вылете героя берёт приз без `table_equities`, так что ветвей не больше
  трёх. §7.3: оценщик — один вызов `eval7.evaluate` на семь карт, карты `eval7` строятся один
  раз на модуль. Число прогонов не уменьшено.
- **Global Constraints.** Один коммит. Новых гардов и текстов нет. `pressure` отказывает
  тем же порядком и теми же текстами, что три функции порознь: `_branches` общий, риск-премия
  идёт раньше bubble factor. Пинит `test_pressure_refuses_like_the_measures_it_joins`.
  Перестановка вызовов в `analyze` (давление теперь до блока `icm`) исхода не меняет: при
  отказе, отличном от `PressureUndefined`, разбор падает и раньше, и теперь.
- **Решения Фазы 7, оценка каждого:**
  1. Эквити из `pressure`, при неопределённом давлении — четвёртый вызов. **Принято.** Нести
     эквити в исключении — значит расширять `PressureUndefined` полем ради редкого случая, а
     узкий `except` (D4) сохранился. Эквити в этом случае считается по удлинённой лесенке
     `ladder`, не по `prizes` — мутант «лесенка лобби» убит
     (`test_undefined_pressure_keeps_the_equity_on_the_protected_ladder`).
  2. Оговорки приёмки п.3. **Приняты.** `1` без соперника — `==`; `≤ 3` на фикстуре, `≤ 6`
     с защитой, `≤ 7` с защитой при неопределённом давлении (3 + 1 + 3) — сверено по коду
     построчно. Все три счётчика краснеют на `analyze.py` из `a42dfee` (13 вызовов).
  3. Два теста подменяют `pressure` вместо `risk_premium`. **Принято:** старое имя из
     `analyze` больше не импортируется, и подмена по нему стала бы пустой. Утверждения не
     менялись.
  4. Лок: `eval7==0.1.11`, `future==1.0.0`, `pyparsing==3.3.3`. **Принято.** `pip show eval7`:
     `Requires: future, pyparsing`; без них `--no-deps` оставил бы `import eval7` сломанным.
     Проверено по PyPI: `eval7 0.1.11` для `win_amd64` есть только `cp312`, для 3.13/3.14 нет,
     sdist нет (`--no-binary` видит только 0.1.2–0.1.10). На Linux колёса есть для cp313–cp315.
     README и шапка лока говорят это верно. Решение по D21 — ниже.
  5. Фильтр `DeprecationWarning` на `eval7.rangestring`. **Принято:** с `-o filterwarnings=""`
     выходят `PyparsingDeprecationWarning` из `rangestring.py`, с фильтром — нет, а чужие
     модули он не глушит. Замечание: `pyparsing` снимает эти имена в 4.0, и `import eval7`
     тогда упадёт. Лок пинит 3.3.3, `pyproject` верхней границы не ставит. С заменой оценщика
     по D21 вопрос снимается сам, поэтому это не находка.
  6. Тесты сверх наброска. **Принято:** ничьи на общей доске (125 из 3000) — единственная
     проверка деления банка.
  7. Файлы сверх списка (`icm_field.py`, `test_analyze.py`, `test_icm_field_pressure.py`,
     README). **Принято.**
- **Тест на 2 секунды краснеет на медленной реализации — перепроверено.** С `equity.py` из
  `a42dfee` тест падает: «разбор занял 7.72 с при бюджете 2.0 с». Остальные четыре теста
  бюджета зелёные. С `analyze.py` из `a42dfee` падают три счётчика: 13 вызовов.
- **Приёмка Задачи 8.** п.1 — `test_the_fast_evaluator_orders_hands_exactly_like_pokerkit`,
  3000 пар. п.2 — `test_equity_crosscheck.py` в `59ac718 --stat` отсутствует. п.3 — четыре
  счётчика в `test_analyze_budget.py`. п.4 — `test_a_full_analysis_fits_the_budget`, 0.15 с на
  этой машине. п.5 — `pyproject.toml` `eval7>=0.1.11`, лок `eval7==0.1.11`. п.6 — `pokerkit>=0.5`
  в `dependencies`; в `src` не импортируется. п.7 — 469 passed.
- **Мутанты Задачи 8** (11 штук, чистый кэш, скрипт ниже). Все убиты: `min` вместо `max` в
  оценщике; ничья без деления банка; доска без последней карты; перестановка риск-премии и
  bubble factor в `pressure`; `win` вместо `now`; `hero_equity` всегда; `except ValueError`
  вместо `PressureUndefined` (D4); эквити при неопределённом давлении по `prizes`; давление без
  возврата по `ladder`; перестановка ключей в `riskPremium` и в `riskPremiumWithoutRefund`.
- **F7.1 (`a42dfee`).** Абзац README о защите на баббле передаёт все три части ответа
  пользователя по D9. Слова «открыта ли регистрация — не важно: `lateRegOpen` на поправку не
  влияет» совпадают с docstring `protected_ladder`. Находок нет. **Задача 7 закрыта.**

Находок в коде Задачи 8 нет. **Задача 8** закрывается, но D21 (ниже) меняет её оценщик, и
Фаза 10 ревьюит эту замену.

### Финальное ревью ветки (`git diff d686089..HEAD`)

Объём: 26 файлов, +4207/−605, 24 коммита. Спека прочитана целиком. Код ядра — `analyze.py`,
`icm_field.py`, `handstate.py` (дифф), `bounty.py`, `ladder.py`, `cli.py`, `equity.py`. Сквозное
— README, `.claude/skills/poker-math/SKILL.md`, CLI.

**§8.1 Вход.** `bubbleRefundUsd` необязателен (`_as_optional_float`, `check_non_negative`
«возврат бай-ина»). `seats[].bountyUsd` необязателен; ценник героя обязателен, как только есть
хоть один ценник (`_validate_prices`), и сверх спеки — у каждого, кто в раздаче. `averageStackBb`
обязателен ровно тогда, когда `players_left > мест за столом` (`analyze._field_stack`). Решение
плана — требовать полноту `payouts` пометкой, а не таблицей «входов → мест».

**§8.2 Выход.** `icm.heroEquity`. Блок `bounty` содержит пять полей спеки (`villainPriceUsd`,
`knockoutCashUsd`, `ownPriceGrowthUsd`, `heroPriceAtRiskUsd`, `requiredEquityWithBounty`) плюс
`bbValueUsd`. Блок `bubbleProtection` — отдельной строкой. Лесенка и головы раздельно: пинит
`test_the_two_halves_of_equity_stay_separate`.

**§8.3 Пометки.** Grep по `src`: `ladder_truncated` и `reduced_field` не встречаются.
`field_homogeneous`, `pko`, `ladder_incomplete`, `bubble_protection` ставятся; `mh_bias`,
`late_reg_open`, `no_pushfold`, `vpip_default`, `icm_pressure_undefined` остались. README
перечисляет те же девять. `field.py` с `reduce_field` жив, но вне расчётного пути: так велит
таблица файлов плана («сам модуль и его тесты остаются»). Кандидат для D13.

**§9 — каждый инвариант и тест, который его пинит:**

| # | Инвариант | Тест |
|---|---|---|
| 1 | деньги не теряются | `test_icm_field.py::test_money_is_never_lost`, `…places_already_paid_out_are_not_handed_out_again` |
| 2 | масштаб лесенки | `test_icm_field.py::test_scaling_the_ladder_scales_every_equity`, `test_icm_field_pressure.py::test_scaling_the_ladder_leaves_pressure_untouched` |
| 3 | масштаб стеков | `test_icm_field.py::test_scaling_every_stack_changes_nothing` (и `test_icm.py`) |
| 4 | F = 0 совпадает с перебором до 1e-12 | `test_icm_field.py::test_an_empty_field_reproduces_the_exact_enumeration` |
| 5 | монотонность, равные — поровну | `…a_bigger_stack_is_worth_more_but_less_than_proportionally`, `…equal_stacks_everywhere_split_the_pool_evenly` |
| 6 | меньше, чем пропорционально | `…a_bigger_stack_is_worth_more_but_less_than_proportionally` |
| 7 | плоская лесенка не давит | `test_icm_field_pressure.py::test_a_flat_ladder_paying_everyone_does_not_press` |
| 8 | бабл давит сильнее | `test_icm_field_pressure.py::test_pressure_is_higher_on_the_bubble_than_deep_in_the_money` |
| 9 | сохранение баунти-фонда | `test_bounty.py::test_the_bounty_pool_is_conserved`, `…a_knockout_moves_the_whole_bounty_of_the_busted` |
| 10 | взятые баунти не влияют | `test_analyze.py::test_the_two_halves_of_equity_stay_separate`: в контракте нет поля для взятых баунти, инвариант держится конструктивно |
| 11 | классика не задета PKO | `test_analyze.py::test_a_table_without_prices_is_not_a_pko`, `…a_classic_table_never_touches_the_bounty_module` |
| 12 | бюджет | `test_analyze_budget.py::test_a_full_analysis_fits_the_budget` |

У каждого инварианта есть тест. Инвариант §6 про возврат `c` (в §9 его нет, он из плана) пинит
`test_a_refund_is_the_same_as_every_paid_prize_lowered_by_it` — сверено в Фазе 7.

**Сквозное.** README: установка с оговоркой про Windows и Python; `analyze` с моделью поля,
пометками, PKO, защитой на баббле, ограничением стека 0; `bounty-ev` без `--split`. `SKILL.md`:
`--bounty` — число с экрана, `--split` нет (D8). CLI: `bounty-ev` без `--split`, `analyze`
через `--input`. Расхождение CLI `icm`/`risk-premium` с `analyze` — D12, ниже.

Одна находка — **F8.1** (ниже): комментарий в `analyze.py` называет winner-take-all примером
неопределённого давления.

### D22 — мутанты «снести гард целиком» на чистом кэше

Скрипт (`ast`): каждый `raise` и каждый вызов `check_*`/`_check*`/`_validate` в модуле по одному
заменяется на `pass` с тем же отступом. Затем `PYTHONDONTWRITEBYTECODE=1`, удаление всех
`__pycache__` в `src` и `tests`, полный сьют `-x -q --tb=no -p no:cacheprovider`, откат из
копии в памяти в `finally` и повторная чистка кэша. Модули: `_checks.py`, `ladder.py`,
`icm_field.py`, `bounty.py`, `analyze.py`, `handstate.py`, `equity.py`, `cli.py`. Перед прогоном
и после него сьют на чистом кэше — 469 passed, `git status` чистый.

**120 мутантов, выжили 11.** Все гарды, которые план 3 добавил или менял, убиты:
`_checks.py` (10/10), `ladder.py` (11/11), `icm_field.py` (15/15), `analyze.py` (6/6),
`bounty.py` (кроме одного, ниже), новые гарды `handstate.py` (возврат, ценники, ладдер-блок,
монотонность, `entrants`). **Вердикты «убит» Фаз 1–6 по гардам подтверждены на чистом кэше.**

Выжившие:

- `bounty.py:77` `check_positive(call_amount)` — **эквивалентный, известный** (журнал Фазы 5):
  `required_equity` на обеих ветках проверяет то же имя тем же текстом.
- `cli.py:139` «неизвестная команда» — **эквивалентный**: `add_subparsers(required=True)`, и
  argparse не пропустит неизвестную команду до `_dispatch`.
- Девять — **не эквивалентны**. Все сидят в коде планов 1–2, который мутант-прогоны плана 3 не
  трогали (Фазы 1–6 мутировали только свои модули). Находки **F8.2–F8.4**.

### Находки

- **F8.1** — `packages/poker-engine/src/poker_engine/analyze.py:160`, комментарий перед
  `pressure`: «Неопределённое давление (winner-take-all, деньги вне достижимых мест, лесенка,
  награждающая вылет)». Про winner-take-all это неправда. На модели поля при WTA давление
  определено: риск-премия 0, bubble factor 1.0 (docstring `icm_field.risk_premium`/`bubble_factor`,
  `test_winner_take_all_has_no_ladder_pressure`). Проверено через `analyze` с лесенкой
  `[{1,1,100}]`, `placesPaid 1`: `riskPremium ≈ 2e-16`, `bubbleFactor ≈ 1.0`, пометки
  `icm_pressure_undefined` нет — ни на 6 живых, ни на 496. Текст пришёл из `178ea65`
  (Задача 4). Чинить: убрать «winner-take-all» из перечня; можно заменить на «возврат больше
  минимального приза».
- **F8.2** — `packages/poker-engine/src/poker_engine/equity.py`, семь гардов, снос которых не
  краснит ни одного теста: `:49` «нужно минимум две руки», `:109` «диапазон соперника пуст»,
  `:115` `_check_duplicates(cards)` комбинации диапазона, `:166` «ожидалось N карт», `:169`
  «неизвестная карта '…' в '…'», `:175` «на доске не может быть больше 5 карт», `:178`
  «неизвестная карта на доске». Причина: `tests/test_equity.py:63,68,79` зовут голый
  `pytest.raises(ValueError)` (нарушение конвенции `match=`) — отказ приходит от другого гарда
  или от `combinations`. На пять текстов тестов нет вовсе. Чинить только тестами: запинить
  `match=` в трёх голых тестах и добавить по отказу на каждый из семи гардов (`hand_equity` с
  одной рукой; `equity_vs_range` с пустым диапазоном; комбинация `AhAh`; рука `AhK`; рука `XxAh`;
  шесть карт доски; карта доски `Xx`). Каждый такой тест краснит свой мутант.
- **F8.3** — `packages/poker-engine/src/poker_engine/handstate.py:261`, `_as_optional_bool`
  (единственный вызов — `isHero`): снос `raise` не краснит ничего. Строка `"false"` в `isHero`
  тогда проходит как истина, и героем может стать не тот. Тесты
  `test_a_string_instead_of_a_boolean_is_rejected` / `…number_instead_of_a_boolean…` бьют только
  `_as_bool` (`inHand`, `lateRegOpen`). Чинить тестом: `isHero: "false"` → `match=^поле 'isHero'
  должно быть true или false`.
- **F8.4** — `packages/poker-engine/src/poker_engine/cli.py:175`, `_str_list`: гард «пустой
  элемент в списке» не пинится ничем. Текст в `test_ranges.py` — другой гард, в `ranges.py`.
  Чинить тестом CLI: `equity --hands AhKh,,AdKd` → `{"error": "пустой элемент в списке: …"}`
  с кодом 1.

Итог ревью: **четыре находки, F8.1–F8.4.** Три из них (F8.2–F8.4) — дыры в тестах кода
планов 1–2, вскрытые перепрогоном D22; код менять не нужно.

### Долги

- **D12 — оценён, решение пользователя.** При пустом поле (F = 0, финальный стол) CLI и
  `analyze` дают одно и то же: `icm.risk_premium`/`bubble_factor` против `icm_field` на стеках
  `[5000, 3000, 2000, 1500, 900]`, выплаты `500/300/200`, три пары мест — расхождение ≤ 3·10⁻¹⁵.
  Эквити пинит инвариант 4. В середине турнира CLI не знает о поле и считает переданный стол
  целым турниром. Это ошибка §2.2 спеки, и числа расходятся в разы. **Решение пользователя
  (2026-09-25): оговорить в `SKILL.md` и README** — `icm`/`risk-premium` верны, только когда
  `--stacks` — весь оставшийся турнир (финальный стол); в середине турнира числа брать из
  `analyze`. Код не меняется. Хозяин — Фаза 9.
- **D21 — оценён, решение пользователя.** Варианты: (а) `.venv` от Python 3.12 — кода не
  меняет, но упрётся снова на 3.13+; (б) `phevaluator 0.6.0` — колёса `win_amd64` cp313 и cp314
  есть (проверено `pip download`), sdist есть, рантайм-зависимостей нет (`Requires-Dist` —
  только extras). Замер: разбор эталонной фикстуры 0.22–0.23 с против 0.14–0.15 с у `eval7`,
  бюджет 2 с. Согласие с `pokerkit` — 3000 пар на общей доске, 125 ничьих, расхождений 0.
  Направление шкалы обратное: меньше — сильнее; (в) сборка `eval7` из git — нужны MSVC и Cython,
  хрупко. **Решение пользователя (2026-09-25): `phevaluator 0.6.0`.** Хозяин — Фаза 9: заменить
  оценщик в `_score_runout` (знак!), `pyproject` и лок (`eval7`, `future`, `pyparsing` убрать,
  `phevaluator==0.6.0` добавить), снять фильтр `DeprecationWarning`, тест согласия перевести на
  `phevaluator`, README и docstring `equity.py`. Итог — в ledger финала.
- **D22 — закрыт этой фазой:** вердикты гардов Фаз 1–6 подтверждены на чистом кэше, выжившие —
  F8.2–F8.4.
- **D1** — 469 до и после. **D16** — `.venv/bin/python`.
- Новых долгов нет. `field.py` вне расчётного пути — к D13 (ledger финала).

### Коммиты фазы

| Коммит | Что |
|---|---|
| этот | `docs`: отчёт Фазы 8, реестр, промпт Фазы 9 |

### Тесты (D1)

До: 469 passed. После: **469 passed**, код не менялся. Полных прогонов — три обычных
(база; перед мутантами на чистом кэше; после мутантов на чистом кэше) и 131 мутант-прогон.

### Где остановились

Фаза доделана. Следующая — Фаза 9: починка F8.1–F8.4, D21 (замена оценщика на
`phevaluator`), D12 (оговорка в `SKILL.md` и README).

## Фаза 9 — починка находок Фазы 8, D21, D12 (2026-09-25)

Ветка `claude/serene-bell-yni6li` (облачная сессия, §3), Linux, `.venv/bin/python` (D16),
Python 3.11.15. База: **469 passed** за 7.8 с (`-o addopts="" -q`), совпадает с промптом.
Фаза ничего не ревьюит: её починки ревьюит Фаза 10.

Мутанты — скриптом из копии в памяти: `PYTHONDONTWRITEBYTECODE=1`, чистка `__pycache__` в
`src` и `tests` перед каждым прогоном и после отката, `-x -q --tb=no -p no:cacheprovider`.
Снос гарда — строка (или весь многострочный `raise`) заменяется на `pass` с тем же отступом.

### Находки Фазы 8

- **F8.1 — починена, `6b5ae01`.** Из комментария перед `pressure` в `analyze.py` убран
  «winner-take-all»; дописано, что при WTA давление на модели поля определено (риск-премия 0,
  bubble factor 1.0). Замену «возврат больше минимального приза», которую предлагала
  находка, не вписал: этот случай я не проверял, а комментарий обязан перечислять только
  доказанное. Остались два случая, которые `_risk_premium_from`/`_bubble_factor_from`
  реально отвергают: деньги вне достижимых мест (`now = 0`) и лесенка, награждающая вылет
  (`win ≤ now`).
- **F8.2 — починена, `033642f`, только тесты.** В `test_equity.py` три голых
  `pytest.raises(ValueError)` запинены через `match=` с якорями `^…$` (дубль карты; доска из
  шести карт — без гарда `combinations` бросает свой `ValueError`; `trials` — текст
  `trials должен быть > 0, получено 0`). Добавлены шесть тестов: одна рука; пустой диапазон
  (якорь `$` отличает его от «пуст после исключения известных карт»); комбинация `AhAh`;
  рука с неверным числом карт; `XxAh`; карта доски `Xx`. **Против текста находки:** для гарда
  «ожидалось N карт» находка предлагала руку `AhK`, но она режется на `["Ah", "K"]` — две
  «карты», число верное, и отказ приходит от гарда «неизвестная карта». Взята рука `Ah`
  (одна карта) — `рука 'Ah': ожидалось 2 карт, вышло 1`. Мутанты — все 13 `raise`/гардов
  `equity.py`: убиты (семь целевых — `test_equity.py`, три гарда `equity_vs_range` —
  `test_equity_vs_range.py`, три прежних — старыми тестами).
- **F8.3 — починена, `6ee250d`, только тест.**
  `test_a_string_instead_of_a_boolean_is_rejected_for_the_hero_flag`: `isHero: "false"` на
  месте 0 → `^поле 'isHero' должно быть true или false, получено 'false'$`. Снос `raise` в
  `_as_optional_bool` (строки 261–263) убит.
- **F8.4 — починена, `0fa13e6`, код и тест. Против текста находки.** Находка ждала
  `{"error": "пустой элемент в списке: …"}`, но на деле CLI отвечал
  `{"error": "argument --hands: invalid _str_list value: 'AhKh,,AdKd'"}`: на `ValueError` из
  `type=` argparse подставляет свой текст, и сообщение гарда до пользователя не доходило
  никогда. Тест одним `match=` на прежнем коде запинил бы английскую заглушку argparse, а
  не гард. Починка: `_str_list` бросает `argparse.ArgumentTypeError` — его текст argparse
  передаёт как есть. Ответ теперь
  `{"error": "argument --hands: пустой элемент в списке: 'AhKh,,AdKd'"}`, код 1; тест
  `test_an_empty_item_in_a_card_list_is_named_in_the_error` пинит ответ целиком. Мутант
  «снос `raise`» убит: без гарда пустой токен доходит до движка как рука `''`. Префикс
  `argument --hands:` добавляет argparse — он называет флаг, и это полезно. `_int_list`/
  `_float_list` не трогал: у них нет своего текста, argparse и так пишет своё
  (`test_embedded_empty_token_in_list_returns_error_json` проверяет только ключ `error`).

### D21 — `phevaluator` вместо `eval7` (`25c3dfd`)

- `equity.py`: `from phevaluator import Card, evaluate_cards`; `_EVAL_CARDS` — номера карт
  `Card(code).id_`, строятся один раз; `_score_runout` зовёт `evaluate_cards(*номера)` и берёт
  **`min`** (меньше — сильнее). Номера, а не строки: 0.08 с против 0.30 с на 100 000 рук.
  Docstring модуля переписан, в нём указано, почему ушли от `eval7`.
- `pyproject.toml`: `phevaluator>=0.6.0`, `filterwarnings` снят. Лок: `phevaluator==0.6.0`;
  `eval7`, `future`, `pyparsing` убраны. В `.venv` они удалены, `pip check` чистый,
  `pip freeze` совпадает с локом построчно. Сьют с `-W error::DeprecationWarning` зелёный.
- `test_equity_evaluator.py`: оба теста сравнивают `-evaluate_cards(*cards)` с `pokerkit`;
  расхождений 0, ничьи на общей доске есть. `test_equity_crosscheck.py` не правился, зелёный.
- README, абзац про оценщик: колёса Windows cp313/cp314 и sdist, `.venv` от любого 3.11+.
- **Бюджет:** разбор фикстуры 0.16 с (Фаза 8 мерила 0.23 с со строками). С `equity.py` из
  `a42dfee` тест бюджета краснеет: «разбор занял 8.02 с при бюджете 2.0 с».
- **Мутанты оценщика, все убиты:** `max` вместо `min`; ничья без деления банка; доска без
  последней карты; номера карт из перевёрнутой колоды; тест согласия без минуса.
- Решение против текста плана — блок «Решения по находкам ревью (Фаза 9, 2026-09-25)» в
  Задаче 8: `eval7` → `phevaluator` в шагах и приёмке, `min`, номера карт.

### D12 — оговорка (`f35ad10`)

`.claude/skills/poker-math/SKILL.md`, «Известные ограничения» — первый пункт: `icm`/
`risk-premium` считают только переданные стеки, верны на финальном столе, в середине турнира
числа брать из `analyze`. README, таблица команд, строка `icm`, `risk-premium` — то же
коротко. Код не менялся.

### Решения против текста плана и находок

1. Задача 8: оценщик `phevaluator`, `min`, номера карт (блок в плане).
2. F8.1: без предложенной замены «возврат больше минимального приза» — не проверено.
3. F8.2: рука `Ah` вместо `AhK` — `AhK` бьёт другой гард.
4. F8.4: код тоже поменян (`ArgumentTypeError`) — текст гарда до пользователя не доходил.

### Долги

- **D21 — закрыт, `25c3dfd`.** Итог — в ledger финала.
- **D12 — закрыт, `f35ad10`.** Итог — в ledger финала.
- **D1** — 469 до, 477 после (+6 `test_equity.py`, +1 `test_handstate.py`, +1 `test_cli.py`).
  **D16** — `.venv/bin/python`.
- Новых долгов нет.

### Коммиты фазы

| Коммит | Что |
|---|---|
| `6b5ae01` | F8.1: комментарий давления в `analyze` |
| `033642f` | F8.2: тесты отказов `equity` |
| `6ee250d` | F8.3: тест `isHero` |
| `0fa13e6` | F8.4: `ArgumentTypeError` в `_str_list` и тест CLI |
| `25c3dfd` | D21: `phevaluator` вместо `eval7` |
| `f35ad10` | D12: оговорка в `SKILL.md` и README |
| этот | `docs`: отчёт Фазы 9, реестр, промпт Фазы 10 |

### Тесты (D1)

До: 469 passed. После: **477 passed** за 8.3 с. Полных прогонов обычных — три (база; после
D21 с `-W error::DeprecationWarning`; финальный на чистом кэше), плюс мутант-прогоны.

### Где остановились

Фаза доделана. Следующая — Фаза 10: повторное ревью шести починок Фазы 9 и финал плана.
