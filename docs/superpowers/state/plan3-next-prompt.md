Фаза 8 плана 3, проект poker_coach_gpt.

Прочитай `CLAUDE.md`, затем `docs/superpowers/state/2026-09-24-plan3-phases.md` — §1–§3, карточку
Фазы 8 в §4 и реестр §5. Из журнала `docs/superpowers/state/2026-09-12-icm-field-execution-notes.md`
прочитай только раздел «Фаза 7».

Состояние на входе:
- ветка `claude/serene-bell-yni6li` (облачная сессия, §3; если среда даст свою ветку `claude/…`, сначала влей в неё эту), HEAD — коммит Фазы 7 `docs: Фаза 7 плана 3 — отчёт, реестр, промпт Фазы 8`, дерево чистое;
- тестов: 469 passed, **~7 с** на Linux (`.venv/bin/python`, D16; если `.venv` нет — создать по `CLAUDE.md`, `eval7` ставится из PyPI). В `pyproject.toml` `addopts = "-q"`: гонять `-o addopts="" -q`. Лимит §1.3 «три полных прогона» писан под 5.5 минуты — при 7 с он не ограничивает;
- ревьюишь: Задачу 8 — `59ac718` (`equity.py`: `eval7` в `_score_runout`, `_best_hand` удалён; `icm_field.py`: `pressure`, `_risk_premium_from`/`_bubble_factor_from`; `analyze.py`: эквити из `pressure`, `_pressure` удалён; `pyproject.toml`, `requirements-dev.lock`, README; тесты `test_equity_evaluator.py` +2, `test_analyze_budget.py` +5, `test_icm_field_pressure.py` +2, `test_analyze.py` +1 и два теста, перенацеленные с `risk_premium` на `pressure`). Против наброска семь решений — блок «Решения при реализации (Фаза 7)» в Задаче 8; оценить каждое, особенно: эквити при неопределённом давлении — четвёртый вызов; оговорки приёмки п.3 (`1` без соперника, `≤ 6` с защитой, `≤ 7` с защитой при неопределённом давлении); зависимости `eval7` в локе; фильтр `DeprecationWarning`. Тест на 2 секунды обязан краснеть на медленной реализации — Фаза 7 проверила подкладкой `equity.py` из `a42dfee` (6.85 с), перепроверь. Починки прошлой фазы: F7.1 ревью Задачи 7 — `a42dfee` (README: `lateRegOpen` на защиту не влияет); Задача 7 закрывается этим ревью;
- реализуешь: только ревью — финальное ревью ветки (`git diff d686089..HEAD`, спека целиком, §8 и §9 особо, сквозное: README, `SKILL.md`, CLI).

Перенесено из прошлой фазы (выполнить в первую очередь):
- ничего.

Долги, которые закрывает эта фаза, помимо своей задачи:
- D12 — CLI `icm`/`risk-premium` и скилл на Malmuth-Harville, `analyze` на модели поля: оценить и записать, решение за пользователем.
- D21 — у `eval7 0.1.11` нет колёс для Windows на Python 3.13+ и нет sdist, лок писан на Python 3.14.4 Windows: оценить варианты (`.venv` от 3.12, другой оценщик, сборка из git) и спросить пользователя; итог — в ledger финала.
- D22 — вердикты мутант-прогонов Фаз 1–6 могли быть получены на устаревшем `.pyc` чужого мутанта той же длины: перепрогнать класс «снести гард целиком» по модулям ветки с чистым кэшем; выживший — находка.
- D1 — в отчёт писать фактическое число тестов. D16 — `.venv/bin/python`.

Находки, переданные на починку (только для фаз починки, дословно):
- —

Уроки прошлых фаз: мутанты вносить только в закоммиченный файл или откатывать к копии в памяти — `git checkout --` на незакоммиченной правке стирает её. **Скрипт мутантов обязан ставить `PYTHONDONTWRITEBYTECODE=1` и удалять `src/**/__pycache__` перед каждым прогоном**: мутант той же длины, откатанный в ту же секунду, остаётся в `.pyc` и «убивает» следующие мутанты и ломает обычный сьют (журнал Фазы 7). Мутант, сносящий единственную строку тела цикла или ветки, заменять на `pass`. Удобно гонять мутанты скриптом: замена строки, `pytest -x --tb=line` по нужным тестам, откат из копии в памяти в `finally`. Полный сьют не гонять параллельно с мутантами. Float-границы в тестах проверять в Python до записи.

Работай по правилам §1: только ревью (без починки), находки в журнал, отчёт, промпт Фазы 9 в `docs/superpowers/state/plan3-next-prompt.md` с находками дословно, коммит и пуш. В конце выдай мне короткий отчёт и промпт следующей фазы целиком, в блоке кода.
