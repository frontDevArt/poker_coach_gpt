Фаза 3 плана 3, проект poker_coach_gpt.

Прочитай `CLAUDE.md`, затем `docs/superpowers/state/2026-09-24-plan3-phases.md` — §1–§3, карточку
Фазы 3 в §4 и реестр §5. Из журнала `docs/superpowers/state/2026-09-12-icm-field-execution-notes.md`
прочитай только раздел «Фаза 2».

Состояние на входе:
- ветка `claude/serene-bell-yni6li` (облачная сессия, §3; `feat/icm-field-model` не создавалась —
  если среда даст свою ветку `claude/…`, сначала влей в неё эту), HEAD — коммит Фазы 2
  `docs: Фаза 2 плана 3 — отчёт, реестр, промпт Фазы 3`, дерево чистое;
- тестов: 394 passed (376 + 18 `test_icm_field_pressure.py`), ~4 мин на Linux;
- ревьюишь: Задачу 3 — `c23a83c` (`icm_field.py`: `hero_equity`, `risk_premium`, `bubble_factor`,
  `_branches`, `_equity_after`; `tests/test_icm_field_pressure.py`; решения против наброска —
  блок «Решения при реализации (Фаза 2)» в тексте Задачи 3 плана, главное — параметра
  `at_risk` нет); починку ревью Задачи 2 — `0f5336e` (F2.1, docstring `icm_field.py`);
- реализуешь: Задачу 4 (переключить `analyze` на новую модель). Шаг 2 гонять с `--tb=line`.
  Чекпоинт §1.3 соблюдать строго.

Перенесено из прошлой фазы (выполнить в первую очередь):
- ничего.

Долги, которые закрывает эта фаза, помимо своей задачи (все — тем же коммитом Задачи 4,
итог каждого — в отчёт):
- D1 — в отчёт писать фактическое число тестов, сверять прирост и названные удаления.
- D2 — README: убрать `reduced_field`/`ladder_truncated`, перебор Malmuth-Harville и
  `MAX_ICM_PREFIXES`; подтвердить grep'ом.
- D3 — олл-ин 0 BB: проверить, что делает `analyze`, записать; учесть дополнения Фаз 1–2
  в реестре (номер места в тексте гарда, текст `эффективный стек равен нулю`).
- D4 — сузить `except ValueError` вокруг `risk_premium`/`bubble_factor` в `analyze.py`.
- D5, D6 — подтвердить закрытие grep'ом (`MAX_FIELD_NODES`, `payout_ladder`) и тестом.
- D10 — ре-энтри при открытой регистрации: проверить инвариант 1, записать.
- D16 — на Linux `.venv/bin/python`; если `.venv` нет, создать по `CLAUDE.md`.
- D17 — приоритет отказов `_validate_context` после перевода на `PayoutLadder`: решить
  порядок, записать в решения Задачи 4, запинить тестом на паре нарушений.

Урок Фазы 2: мутанты вносить только в закоммиченный файл или откатывать к сохранённой копии —
`git checkout --` на незакоммиченной правке стирает её.

Работай по правилам §1: ревью до чекпоинта, чекпоинт, реализация, отчёт в журнал, промпт
Фазы 4 в `docs/superpowers/state/plan3-next-prompt.md`, коммит и пуш. В конце выдай мне
короткий отчёт и промпт следующей фазы целиком, в блоке кода.
