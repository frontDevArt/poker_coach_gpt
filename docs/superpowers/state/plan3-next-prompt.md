Фаза 7 плана 3, проект poker_coach_gpt.

Прочитай `CLAUDE.md`, затем `docs/superpowers/state/2026-09-24-plan3-phases.md` — §1–§3, карточку
Фазы 7 в §4 и реестр §5. Из журнала `docs/superpowers/state/2026-09-12-icm-field-execution-notes.md`
прочитай только раздел «Фаза 6».

Состояние на входе:
- ветка `claude/serene-bell-yni6li` (облачная сессия, §3; если среда даст свою ветку
  `claude/…`, сначала влей в неё эту), HEAD — коммит Фазы 6
  `docs: Фаза 6 плана 3 — отчёт, реестр, промпт Фазы 7`, дерево чистое;
- тестов: 459 passed, ~5 мин на Linux (`.venv/bin/python`, D16). В `pyproject.toml`
  `addopts = "-q"`: гонять `-o addopts="" -q`, иначе итоговой строки с числом нет;
- ревьюишь: Задачу 7 — `03aa820` (`handstate.py`: `TournamentContext.bubble_refund_usd`,
  `protected_ladder`, `_intervals`, гард возврата; `analyze.py`: `prizes`/`ladder`, пометка
  `bubble_protection`, блок `bubbleProtection`, `_pressure`; `test_analyze.py` +14,
  `test_handstate.py` +5; README). Против наброска принято семь решений — блок «Решения при
  реализации (Фаза 6)» в тексте Задачи 7; оценить каждое, особенно: удлинение только при
  `playersLeft > placesPaid`; `ladder_incomplete` и `bbValueUsd` по лесенке лобби, а не по
  удлинённой; возврат больше минимального приза не отвергается; форма
  `riskPremiumWithoutRefund`. D9 закрыт ответом пользователя до реализации (журнал Фазы 6) —
  сверить, что docstring и README говорят ровно это. Починки прошлой фазы: F6.1 ревью
  Задачи 6 — `b15f8e8` (`test_analyze.py`: граница `can_bust`, код не менялся); Задача 6
  закрывается этим ревью;
- реализуешь: Задачу 8 (бюджет двух секунд: `eval7` на горячем пути, три вызова ICM вместо
  семи, тест на 2 секунды). Нужна сеть до PyPI для `eval7`.

Перенесено из прошлой фазы (выполнить в первую очередь):
- ничего.

Долги, которые закрывает эта фаза, помимо своей задачи:
- D7 — шаг 6 Задачи 8: три вызова ICM вместо семи; подтвердить тестом на счётчик
  `table_equities`, а не глазами.
- D20 — при защите на баббле давление считается второй раз, по лесенке лобби: приёмку п.3
  Задачи 8 оговорить для этого случая (три на лесенку), `analyze._pressure` заменить на
  `icm_field.pressure`; тем же коммитом Задачи 8.
- D4 (контроль) — после сведения вызовов в три `except` ловит только `PressureUndefined`;
  `test_only_undefined_pressure_is_turned_into_a_flag` зелёный.
- D1 — в отчёт писать фактическое число тестов, сверять прирост и названные удаления.
- D16 — на Linux `.venv/bin/python`; если `.venv` нет, создать по `CLAUDE.md`.

Замеры «до» (время `analyze` на эталонной фикстуре) снять **до** правок горячего пути.

Уроки прошлых фаз: мутанты вносить только в закоммиченный файл или откатывать к копии в
памяти — `git checkout --` на незакоммиченной правке стирает её. Мутант, сносящий единственную
строку тела цикла или ветки, заменять на `pass`, иначе «убит» синтаксической ошибкой. Удобно
гонять мутанты скриптом: замена строки, `pytest -x --tb=line` по нужным тестам, откат из копии
в памяти в `finally`. Мутант-прогон по `test_analyze.py` с `-k` по нужным тестам — 2–40 с, по
`test_handstate.py` — доли секунды. Полный сьют не гонять параллельно с мутантами — мутант
правит исходник, который читает прогон. Float-границы в тестах проверять в Python до записи
(`11.3 + 1.0 == 5.0 + 7.3` верно, `11.29 + 1.0` — нет).

Работай по правилам §1: ревью до чекпоинта, чекпоинт, реализация, отчёт в журнал, промпт
Фазы 8 в `docs/superpowers/state/plan3-next-prompt.md`, коммит и пуш. В конце выдай мне
короткий отчёт и промпт следующей фазы целиком, в блоке кода.
