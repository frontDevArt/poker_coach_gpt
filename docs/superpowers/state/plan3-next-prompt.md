План 4 проекта poker_coach_gpt: push/fold советы (Nash + ICM-поправка).

План 3 закрыт: модель ICM «стол + однородное поле», настоящая лесенка, PKO в `analyze`, защита на баббле, разбор за 0.16 с (`phevaluator`); 477 passed за ~8 с; ветка `claude/serene-bell-yni6li` влита в `main` fast-forward (2026-09-25).

Прочитай `CLAUDE.md`, затем:
- `docs/superpowers/specs/2026-09-09-poker-skillpack-design.md` — §5 (слой L2), §8 (`pushfold.py`),
  §9 (приёмка T2: HU push/fold против публичных чартов), §10 (фаза Ф2), §11 (блокеры пользователя);
- `docs/superpowers/specs/2026-09-12-icm-field-model-design.md`, §10 и §11;
- журнал `docs/superpowers/state/2026-09-12-icm-field-execution-notes.md` — итоговый раздел:
  ledger отложенного и открытые вопросы.

Открытые вопросы, которые план 4 обязан решить или явно отложить:
- D3 — соперник в олл-ине, стек 0 BB (спека плана 3, §11.1): `analyze` отвергает стек 0 до ICM текстом `стек на месте {seatIndex} должен быть > 0`; модель (как учитывать фишки, уже вложенные выбывающим) не выбрана — решение за пользователем. Следствие: колл против настоящего олл-ина — главный спот PKO и push/fold — не получает `requiredEquityWithBounty`.
- D11 — разброс стеков поля (спека плана 3, §11.4): однородность поля — допущение, оценить можно только вторым движком (Monte-Carlo, вне скоупа плана 3); до тех пор пометка `field_homogeneous`.
- D12 — CLI `icm`/`risk-premium` и скилл `poker-math` на Malmuth-Harville без поля: при F = 0 совпадают с `analyze`, в середине турнира считают стол целым турниром. Сейчас — оговорка в `SKILL.md` и README (`f35ad10`). Если ICM-поправка push/fold пойдёт через CLI или скилл — брать модель поля.
- D13 — мелочи для будущего `refactor(engine)`: `seats_per_table` не используется; `positions_for` в `_validate_node` — выражение-стейтмент; нет гарда «банк не убывает»; `handstate.py` пора делить; `effectiveStackBb` без вложенного; `TableSnapshot`; ссылка скиллов на `poker-review-method`; инвариант T1; `field.py` (`reduce_field`) и его тесты вне расчётного пути `analyze`; `hero_equity([], …)` называет диапазон `[0, -1]`.
- D19 — абсолютный допуск `abs_tol=1e-9` в `risk_premium`/`bubble_factor` (`icm.py`, `icm_field.py`) ломает инвариант масштаба на призах порядка `1e-9`; реальные призы от центов, не достижимо; тексты и логика — контракт CLI.
- D15 — ветки `feat/poker-engine-core` и `feat/screenshot-coach-engine` на GitHub не удалены (из облачной сессии удаление обрывается) — за пользователем.
- Закрыты планом 3, для справки: D10 (ре-энтри не ломают инвариант 1), D21 (`phevaluator` вместо `eval7`), спека плана 3 §11.2 (условия защиты на баббле, ответ пользователя 2026-09-24).

Порядок работы: сначала спека через брейнсторм со мной (вопросы по одному), затем план.
План сразу режь на фазы по правилам `docs/superpowers/state/2026-09-24-plan3-phases.md`:
одна фаза — одна сессия до ~150k, ревью задачи делает следующая фаза, реестр долгов с
хозяевами, промпт следующей фазы в конце каждой сессии. Кода в этой сессии не пиши.
