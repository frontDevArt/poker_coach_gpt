Фаза 6 плана 3, проект poker_coach_gpt.

Прочитай `CLAUDE.md`, затем `docs/superpowers/state/2026-09-24-plan3-phases.md` — §1–§3, карточку
Фазы 6 в §4 и реестр §5. Из журнала `docs/superpowers/state/2026-09-12-icm-field-execution-notes.md`
прочитай только раздел «Фаза 5».

Состояние на входе:
- ветка `claude/serene-bell-yni6li` (облачная сессия, §3; если среда даст свою ветку
  `claude/…`, сначала влей в неё эту), HEAD — коммит Фазы 5
  `docs: Фаза 5 плана 3 — отчёт, реестр, промпт Фазы 6`, дерево чистое;
- тестов: 439 passed, ~4.5 мин на Linux (`.venv/bin/python`, D16). В `pyproject.toml`
  `addopts = "-q"`: гонять `-o addopts="" -q`, иначе итоговой строки с числом нет;
- ревьюишь: Задачу 6 — `61e57fd` (`handstate.py`: `Seat.bounty_usd`, `_validate_prices`;
  `analyze.py`: пометка `pko`, `_bounty_block`; `test_analyze.py` +16, `test_handstate.py` +9;
  README). Против наброска принято семь решений — блок «Решения при реализации (Фаза 5)» в
  тексте Задачи 6; оценить каждое, особенно: ценник обязателен у всех в раздаче, а не только у
  героя; `bbValueUsd` из призов мест `1..min(playersLeft, placesPaid)` вместо
  `ladder.total()`; условие «нокаут возможен» (колл накрывает остаток и герой покрывает
  соперника с вложенным); плановый тест «голова снижает порог» переписан, потому что на
  фикстуре был бы красным. Починки прошлой фазы: F5.1 ревью Задачи 5 — `8616497`
  (`test_bounty.py`: гард `chip_value` без покрытия, `match=` у пяти отказов); Задача 5
  закрывается этим ревью;
- реализуешь: Задачу 7 (защита на баббле: `TournamentContext.bubble_refund_usd`, ключ
  `bubbleProtection` в ответе, пометка `bubble_protection`).

Перенесено из прошлой фазы (выполнить в первую очередь):
- ничего.

Долги, которые закрывает эта фаза, помимо своей задачи:
- D9 — **до** реализации Задачи 7 спросить пользователя про условия защиты на баббле (спека
  §11.2: кто считается ранней пташкой, где кончается бабл). Нет ответа — реализовать по плану,
  а в отчёте и в ответе `analyze` оставить открытым вопросом.
- D1 — в отчёт писать фактическое число тестов, сверять прирост и названные удаления.
- D16 — на Linux `.venv/bin/python`; если `.venv` нет, создать по `CLAUDE.md`.

Уроки прошлых фаз: мутанты вносить только в закоммиченный файл или откатывать к копии в
памяти — `git checkout --` на незакоммиченной правке стирает её. Мутант, сносящий единственную
строку тела цикла или ветки, заменять на `pass`, иначе «убит» синтаксической ошибкой. Мутант-прогон
по `test_analyze.py` с `-k` по нужным тестам — секунды–20 с, по `test_bounty.py` и
`test_handstate.py` — доли секунды. Полный сьют не гонять параллельно с мутантами — мутант
правит исходник, который читает прогон.

Работай по правилам §1: ревью до чекпоинта, чекпоинт, реализация, отчёт в журнал, промпт
Фазы 7 в `docs/superpowers/state/plan3-next-prompt.md`, коммит и пуш. В конце выдай мне
короткий отчёт и промпт следующей фазы целиком, в блоке кода.
