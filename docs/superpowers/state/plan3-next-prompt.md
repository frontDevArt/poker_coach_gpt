Фаза 2 плана 3, проект poker_coach_gpt.

Прочитай `CLAUDE.md`, затем `docs/superpowers/state/2026-09-24-plan3-phases.md` — §1–§3, карточку
Фазы 2 в §4 и реестр §5. Из журнала `docs/superpowers/state/2026-09-12-icm-field-execution-notes.md`
прочитай только раздел «Фаза 1».

Состояние на входе:
- ветка `claude/serene-bell-yni6li` (облачная сессия Фазы 1 работала в ней, §3; `feat/icm-field-model`
  не создавалась — если среда даст свою ветку `claude/…`, сначала влей в неё эту), HEAD — коммит
  Фазы 1 `docs: Фаза 1 плана 3 — отчёт, реестр, промпт Фазы 2`, дерево чистое;
- тестов: 376 passed (357 + 19 `test_icm_field.py`), ~4 мин на Linux;
- ревьюишь: Задачу 2 — `9ea17e0` (`icm_field.py`, `tests/test_icm_field.py`; решения против
  наброска — блок «Решения при реализации (Фаза 1)» в тексте Задачи 2 плана); починки прогона E
  Задачи 1 — `2c6dbb8` (находки E1, E2); вне ревью Задачи 1, но тоже ревьюишь — `dac3376`
  (тест CLI без `.exe` вне Windows, долг D18);
- реализуешь: Задачу 3 (риск-премия и bubble factor на новой модели). Тексты обеих ошибок
  переносятся из `icm.py` дословно — это контракт CLI.

Перенесено из прошлой фазы (выполнить в первую очередь):
- ничего.

Долги, которые закрывает эта фаза, помимо своей задачи:
- D1 — в отчёт писать фактическое число тестов; ожидаемые числа плана устарели, сверять прирост.
- D16 — на Linux команды через `.venv/bin/python`; если `.venv` нет, создать по `CLAUDE.md`.

Работай по правилам §1: ревью до чекпоинта, чекпоинт, реализация, отчёт в журнал, промпт
Фазы 3 в `docs/superpowers/state/plan3-next-prompt.md`, коммит и пуш. В конце выдай мне
короткий отчёт и промпт следующей фазы целиком, в блоке кода.
