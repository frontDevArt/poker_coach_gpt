Фаза 5 плана 3, проект poker_coach_gpt.

Прочитай `CLAUDE.md`, затем `docs/superpowers/state/2026-09-24-plan3-phases.md` — §1–§3, карточку
Фазы 5 в §4 и реестр §5. Из журнала `docs/superpowers/state/2026-09-12-icm-field-execution-notes.md`
прочитай только раздел «Фаза 4».

Состояние на входе:
- ветка `claude/serene-bell-yni6li` (облачная сессия, §3; если среда даст свою ветку
  `claude/…`, сначала влей в неё эту), HEAD — коммит Фазы 4
  `docs: Фаза 4 плана 3 — отчёт, реестр, промпт Фазы 5`, дерево чистое;
- тестов: 413 passed, ~4 мин на Linux (`.venv/bin/python`, D16). В `pyproject.toml`
  `addopts = "-q"`, поэтому `pytest -q` даёт `-qq` без итоговой строки с числом: гонять
  `-o addopts="" -q`;
- ревьюишь: Задачу 5 — `37710bd` (`bounty.py`: `knockout_cash = bounty`, новые
  `own_bounty_growth` и `PROGRESSIVE_SHARE`, `split` убран отовсюду; `cli.py` без `--split`;
  `test_bounty.py`, `test_cli.py`; README; `SKILL.md` по D8; решения против наброска — блок
  «Решения при реализации (Фаза 4)» в тексте Задачи 5, четыре пункта). Приёмку п.4 («README
  не упоминает `--split`») Фаза 4 нарушила сознательно — README называет убранный флаг,
  иначе ломающее изменение не видно; оценить довод. Починки прошлой фазы: F4.1 ревью
  Задачи 4 — `573e12c` (docstring `analyze`, README, тест
  `test_an_incomplete_ladder_out_of_reach_is_flagged_but_costs_nothing`); Задача 4
  закрывается этим ревью;
- реализуешь: Задачу 6 (PKO в разборе: `Seat.bounty_usd`, блок `bounty` в ответе
  `analyze`, пометка `pko`).

Перенесено из прошлой фазы (выполнить в первую очередь):
- ничего.

Долги, которые закрывает эта фаза, помимо своей задачи:
- D1 — в отчёт писать фактическое число тестов, сверять прирост и названные удаления.
- D16 — на Linux `.venv/bin/python`; если `.venv` нет, создать по `CLAUDE.md`.

Уроки прошлых фаз: мутанты вносить только в закоммиченный файл или откатывать к копии в
памяти — `git checkout --` на незакоммиченной правке стирает её. Мутант-прогон по одному
`test_analyze.py` идёт ~20 с на мутанта, по `test_bounty.py` — секунды: выбирать тестовые
файлы под мутируемый модуль. Полный сьют не гонять параллельно с мутантами — мутант
правит исходник, который читает прогон.

Работай по правилам §1: ревью до чекпоинта, чекпоинт, реализация, отчёт в журнал, промпт
Фазы 6 в `docs/superpowers/state/plan3-next-prompt.md`, коммит и пуш. В конце выдай мне
короткий отчёт и промпт следующей фазы целиком, в блоке кода.
