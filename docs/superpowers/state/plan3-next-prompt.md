Фаза 10 плана 3, проект poker_coach_gpt.

Прочитай `CLAUDE.md`, затем `docs/superpowers/state/2026-09-24-plan3-phases.md` — §1–§3, карточку
Фазы 10 в §4, реестр §5 и шаблон §7. Из журнала `docs/superpowers/state/2026-09-12-icm-field-execution-notes.md`
прочитай только раздел «Фаза 9».

Состояние на входе:
- ветка `claude/serene-bell-yni6li` (облачная сессия, §3; если среда даст свою ветку `claude/…`, сначала влей в неё эту), HEAD — коммит Фазы 9 `docs: Фаза 9 плана 3 — отчёт, реестр, промпт Фазы 10`, дерево чистое;
- тестов: 477 passed, ~8 с на Linux (`.venv/bin/python`, D16; если `.venv` нет — создать по `CLAUDE.md`, лок теперь с `phevaluator` вместо `eval7`, нужна сеть к PyPI). В `pyproject.toml` `addopts = "-q"`: гонять `-o addopts="" -q`;
- ревьюишь: все починки Фазы 9, `git diff 0cb129f..f35ad10`, шесть коммитов: `6b5ae01` (F8.1, комментарий в `analyze.py`), `033642f` (F8.2, тесты `test_equity.py`), `6ee250d` (F8.3, тест `isHero`), `0fa13e6` (F8.4 — **поменян и код**: `_str_list` бросает `argparse.ArgumentTypeError`, тест CLI), `25c3dfd` (D21, `phevaluator` вместо `eval7`: `equity.py`, `pyproject.toml`, лок, `test_equity_evaluator.py`, README, блок «Решения по находкам ревью (Фаза 9, 2026-09-25)» в Задаче 8 плана), `f35ad10` (D12, оговорка в `SKILL.md` и README);
- реализуешь: финал плана 3 (карточка Фазы 10, пункты 1–4) — только после `НАХОДОК НЕТ`.

Перенесено из прошлой фазы (выполнить в первую очередь):
- ничего.

Долги, которые закрывает эта фаза, помимо своей задачи:
- D14 — `CLAUDE.md`: «план 3 закрыт», таблица документов, точка входа, число тестов и время прогона (строка «304 passed, ~5.5 мин» устарела), `HANDOFF.md` пометить историческим.
- Ledger финала: D3 (модель стека 0), D10 (ре-энтри, проверен Фазой 3), D11, D13 (с `field.py` вне расчётного пути), D19, D12 и D21 (оба закрыты Фазой 9 — записать итог), D15 (за пользователем).
- D1 — в отчёт писать фактическое число тестов. D16 — `.venv/bin/python`.

Находки, переданные на починку (только для фаз починки, дословно):
- нет.

Что проверить в ревью особо:
- F8.1: в комментарии нет «winner-take-all»; предложенную находкой замену Фаза 9 сознательно не вписала — оценить довод.
- F8.2: каждый из семи гардов `equity.py` краснит свой тест; рука `Ah` вместо `AhK` из находки — оценить довод.
- F8.4: решение против текста находки — код поменян, потому что на `ValueError` из `type=` argparse подставлял свой текст. Проверить, что никакой другой тест или скилл не опирался на прежний английский текст.
- D21: `_score_runout` берёт `min`; тест согласия с `pokerkit` (оба теста) и `test_equity_crosscheck.py` без правок; тест бюджета краснеет на `equity.py` из `a42dfee` (Фаза 9: 8.02 с); `pip freeze` совпадает с локом; README и docstring.

Уроки прошлых фаз: мутанты вносить только в закоммиченный файл или откатывать к копии в памяти. Скрипт мутантов обязан ставить `PYTHONDONTWRITEBYTECODE=1` и удалять `__pycache__` в `src` и `tests` перед каждым прогоном. Многострочный `raise` сносить целиком.

Работай по правилам §1: ревью до чекпоинта, итог ревью в журнал до починки; при находках — починка и повторное ревью в Фазе 10b; при `НАХОДОК НЕТ` — финал: итоговый раздел журнала, D14 в `CLAUDE.md`, вопрос мне про вливание ветки в `main` (fast-forward), промпт плана 4 по шаблону §7 в `docs/superpowers/state/plan3-next-prompt.md`, коммит и пуш. В конце выдай мне короткий отчёт и промпт следующей фазы (или плана 4) целиком, в блоке кода.
