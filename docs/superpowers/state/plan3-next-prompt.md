Фаза 4 плана 3, проект poker_coach_gpt.

Прочитай `CLAUDE.md`, затем `docs/superpowers/state/2026-09-24-plan3-phases.md` — §1–§3, карточку
Фазы 4 в §4 и реестр §5. Из журнала `docs/superpowers/state/2026-09-12-icm-field-execution-notes.md`
прочитай только раздел «Фаза 3».

Состояние на входе:
- ветка `claude/serene-bell-yni6li` (облачная сессия, §3; если среда даст свою ветку
  `claude/…`, сначала влей в неё эту), HEAD — коммит Фазы 3
  `docs: Фаза 3 плана 3 — отчёт, реестр, промпт Фазы 4`, дерево чистое;
- тестов: 404 passed, ~4 мин на Linux (`.venv/bin/python`, D16);
- ревьюишь: Задачу 4 — `178ea65` (`analyze.py` на `icm_field` и `PayoutLadder`; вне списка
  файлов плана — `handstate.py` с `TournamentContext.ladder()` и новым `_validate_context`,
  `ladder.py` docstring, `icm_field.PressureUndefined`, README, `test_cli.py`; решения против
  наброска — блок «Решения при реализации (Фаза 3)» в тексте Задачи 4, девять пунктов).
  Приёмку п.1 проверять grep'ом по каждой строке, п.5 — по сообщению коммита: там названы
  все 13 удалённых тестов. Проверить и закрытые тем же коммитом долги D2–D6, D17 по записям
  журнала. Починок прошлой фазы нет (ревью Задачи 3 — `НАХОДОК НЕТ`, Задача 3 закрыта);
- реализуешь: Задачу 5 (модель половины в `bounty.py`, убрать `--split` из CLI).

Перенесено из прошлой фазы (выполнить в первую очередь):
- ничего.

Долги, которые закрывает эта фаза, помимо своей задачи:
- D1 — в отчёт писать фактическое число тестов, сверять прирост и названные удаления.
- D8 — скилл `poker-math` (`.claude/skills/poker-math/SKILL.md:37,57`): пример `bounty-ev` и
  правило PKO сверить с новой моделью половины, тем же коммитом Задачи 5.
- D16 — на Linux `.venv/bin/python`; если `.venv` нет, создать по `CLAUDE.md`.

Уроки прошлых фаз: мутанты вносить только в закоммиченный файл или откатывать к копии в
памяти — `git checkout --` на незакоммиченной правке стирает её. Мутант-прогон по
`test_analyze.py` + `test_handstate.py` + `test_cli.py` идёт ~20 с на мутанта: выбирать
тестовые файлы под мутируемый модуль.

Работай по правилам §1: ревью до чекпоинта, чекпоинт, реализация, отчёт в журнал, промпт
Фазы 5 в `docs/superpowers/state/plan3-next-prompt.md`, коммит и пуш. В конце выдай мне
короткий отчёт и промпт следующей фазы целиком, в блоке кода.
